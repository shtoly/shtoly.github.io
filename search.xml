<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2025.11.07面试</title>
    <url>/2025/11/07/2025.11.07%E9%9D%A2%E8%AF%95/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>2025.11.07面试</h1>
<h2 id="jQuery选择的标签和document获取的标签有什么区别">jQuery选择的标签和document获取的标签有什么区别</h2>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>jQuery选择器</strong>返回的是<strong>jQuery对象</strong>，它封装了DOM元素并提供了一套丰富的API方法，支持链式调用，但性能相对较低，体积较大。</p>
</li>
<li class="lvl-2">
<p><strong>document方法</strong>返回的是<strong>原生DOM元素或NodeList</strong>，需要使用原生JavaScript方法操作，性能更好，但代码相对繁琐，需要处理浏览器兼容性问题。</p>
</li>
</ul>
<h3 id="核心区别">核心区别</h3>
<table>
<thead>
<tr>
<th>特性</th>
<th>jQuery 选择</th>
<th>document 选择</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>返回类型</strong></td>
<td>jQuery 对象</td>
<td>DOM 元素/NodeList</td>
</tr>
<tr>
<td><strong>方法调用</strong></td>
<td>jQuery 方法</td>
<td>原生 DOM 方法</td>
</tr>
<tr>
<td><strong>链式调用</strong></td>
<td>支持</td>
<td>不支持</td>
</tr>
<tr>
<td><strong>浏览器兼容</strong></td>
<td>自动处理</td>
<td>需要手动处理</td>
</tr>
<tr>
<td><strong>性能</strong></td>
<td>相对较慢</td>
<td>更快</td>
</tr>
<tr>
<td><strong>内存占用</strong></td>
<td>更大</td>
<td>更小</td>
</tr>
</tbody>
</table>
<h3 id="代码示例">代码示例</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 选择元素</span></span><br><span class="line"><span class="comment">// jQuery</span></span><br><span class="line"><span class="keyword">const</span> $elements = $(<span class="string">&#x27;.my-class&#x27;</span>); <span class="comment">// 返回 jQuery 对象</span></span><br><span class="line"><span class="comment">// document</span></span><br><span class="line"><span class="keyword">const</span> elements = <span class="variable language_">document</span>.<span class="title function_">querySelectorAll</span>(<span class="string">&#x27;.my-class&#x27;</span>); <span class="comment">// 返回 NodeList</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 操作元素</span></span><br><span class="line"><span class="comment">// jQuery - 链式调用</span></span><br><span class="line">$(<span class="string">&#x27;.my-class&#x27;</span>)</span><br><span class="line">  .<span class="title function_">css</span>(<span class="string">&#x27;color&#x27;</span>, <span class="string">&#x27;red&#x27;</span>)</span><br><span class="line">  .<span class="title function_">addClass</span>(<span class="string">&#x27;active&#x27;</span>)</span><br><span class="line">  .<span class="title function_">on</span>(<span class="string">&#x27;click&#x27;</span>, handler);</span><br><span class="line"></span><br><span class="line"><span class="comment">// document - 需要遍历</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">querySelectorAll</span>(<span class="string">&#x27;.my-class&#x27;</span>).<span class="title function_">forEach</span>(<span class="function"><span class="params">element</span> =&gt;</span> &#123;</span><br><span class="line">  element.<span class="property">style</span>.<span class="property">color</span> = <span class="string">&#x27;red&#x27;</span>;</span><br><span class="line">  element.<span class="property">classList</span>.<span class="title function_">add</span>(<span class="string">&#x27;active&#x27;</span>);</span><br><span class="line">  element.<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, handler);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 事件处理</span></span><br><span class="line"><span class="comment">// jQuery - 统一的事件处理</span></span><br><span class="line">$(<span class="string">&#x27;.btn&#x27;</span>).<span class="title function_">on</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>($(<span class="variable language_">this</span>).<span class="title function_">text</span>()); <span class="comment">// this 指向 DOM 元素</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// document - 原生事件</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;.btn&#x27;</span>).<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="variable language_">this</span>.<span class="property">textContent</span>); <span class="comment">// this 指向 DOM 元素</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. 属性操作</span></span><br><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">&#x27;#input&#x27;</span>).<span class="title function_">val</span>(); <span class="comment">// 获取值</span></span><br><span class="line">$(<span class="string">&#x27;#input&#x27;</span>).<span class="title function_">val</span>(<span class="string">&#x27;new value&#x27;</span>); <span class="comment">// 设置值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// document</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;input&#x27;</span>).<span class="property">value</span>; <span class="comment">// 获取值</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;input&#x27;</span>).<span class="property">value</span> = <span class="string">&#x27;new value&#x27;</span>; <span class="comment">// 设置值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 5. 创建元素</span></span><br><span class="line"><span class="comment">// jQuery</span></span><br><span class="line"><span class="keyword">const</span> $newElement = $(<span class="string">&#x27;&lt;div&gt;&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">class</span>: <span class="string">&#x27;new-div&#x27;</span>,</span><br><span class="line">  <span class="attr">text</span>: <span class="string">&#x27;Hello World&#x27;</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// document</span></span><br><span class="line"><span class="keyword">const</span> newElement = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&#x27;div&#x27;</span>);</span><br><span class="line">newElement.<span class="property">className</span> = <span class="string">&#x27;new-div&#x27;</span>;</span><br><span class="line">newElement.<span class="property">textContent</span> = <span class="string">&#x27;Hello World&#x27;</span>;</span><br></pre></td></tr></table></figure>
<h3 id="相互转换">相互转换</h3>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery → DOM</span></span><br><span class="line"><span class="keyword">const</span> $div = $(<span class="string">&#x27;.my-div&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> domElement = $div[<span class="number">0</span>]; <span class="comment">// 方法1</span></span><br><span class="line"><span class="keyword">const</span> domElement = $div.<span class="title function_">get</span>(<span class="number">0</span>); <span class="comment">// 方法2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// DOM → jQuery</span></span><br><span class="line"><span class="keyword">const</span> div = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;.my-div&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> $div = $(div);</span><br></pre></td></tr></table></figure>
<h3 id="使用场景建议">使用场景建议</h3>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>jQuery</strong>：快速开发、需要兼容老浏览器、大量DOM操作</p>
</li>
<li class="lvl-2">
<p><strong>document</strong>：现代浏览器、性能要求高、项目体积敏感</p>
</li>
</ul>
<h2 id="怎么实现vue中的v-model">怎么实现vue中的v-model</h2>
<p><strong>v-model的本质是语法糖</strong>，它实际上是<code>value</code>属性绑定和<code>input</code>事件监听的组合。</p>
<p><strong>实现原理：</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>在组件内部通过<code>props</code>接收<code>modelValue</code>（Vue3）或<code>value</code>（Vue2）</p>
</li>
<li class="lvl-2">
<p>当数据变化时，通过<code>$emit</code>触发<code>update:modelValue</code>（Vue3）或<code>input</code>（Vue2）事件</p>
</li>
<li class="lvl-2">
<p>父组件通过v-model自动完成双向数据绑定</p>
</li>
</ul>
<p><strong>关键点：</strong> 要正确定义props和emits，确保数据流的双向同步。</p>
<h3 id="v-model-的本质">v-model 的本质</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!-- 这行代码 --&gt;</span><br><span class="line">&lt;input v-model=&quot;message&quot;&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- 实际上是以下代码的语法糖 --&gt;</span><br><span class="line">&lt;input </span><br><span class="line">  :value=&quot;message&quot; </span><br><span class="line">  @input=&quot;message = $event.target.value&quot;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure>
<h3 id="自定义组件实现-v-model">自定义组件实现 v-model</h3>
<h4 id="Vue-2-实现">Vue 2 实现</h4>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!-- 自定义输入组件 CustomInput.vue --&gt;</span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;input</span><br><span class="line">    :value=&quot;value&quot;</span><br><span class="line">    @input=&quot;$emit(&#x27;input&#x27;, $event.target.value)&quot;</span><br><span class="line">    @blur=&quot;$emit(&#x27;blur&#x27;)&quot;</span><br><span class="line">  &gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script&gt;</span><br><span class="line">export default &#123;</span><br><span class="line">  props: [&#x27;value&#x27;],</span><br><span class="line">  // 或者明确声明</span><br><span class="line">  // model: &#123;</span><br><span class="line">  //   prop: &#x27;value&#x27;,</span><br><span class="line">  //   event: &#x27;input&#x27;</span><br><span class="line">  // &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- 使用 --&gt;</span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;CustomInput v-model=&quot;username&quot; /&gt;</span><br><span class="line">    &lt;!-- 等价于 --&gt;</span><br><span class="line">    &lt;CustomInput :value=&quot;username&quot; @input=&quot;username = $event&quot; /&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure>
<h4 id="vue3实现">vue3实现</h4>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!-- 自定义输入组件 CustomInput.vue --&gt;</span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;input</span><br><span class="line">    :value=&quot;modelValue&quot;</span><br><span class="line">    @input=&quot;$emit(&#x27;update:modelValue&#x27;, $event.target.value)&quot;</span><br><span class="line">  &gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">defineProps([&#x27;modelValue&#x27;])</span><br><span class="line">defineEmits([&#x27;update:modelValue&#x27;])</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;!-- 使用 --&gt;</span><br><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;CustomInput v-model=&quot;username&quot; /&gt;</span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure>
<h2 id="表单提交优化（多次提交表单）">表单提交优化（多次提交表单）</h2>
<p><strong>防重复提交的核心思路是状态控制：</strong></p>
<p><strong>前端层面：</strong></p>
<ol>
<li class="lvl-3">
<p><strong>按钮禁用</strong>：提交期间禁用按钮，防止重复点击</p>
</li>
<li class="lvl-3">
<p><strong>加载状态</strong>：显示loading状态，提升用户体验</p>
</li>
<li class="lvl-3">
<p><strong>请求标记</strong>：为每个请求生成唯一标识，确保处理最新请求</p>
</li>
<li class="lvl-3">
<p><strong>防抖处理</strong>：设置提交间隔，避免短时间多次提交</p>
</li>
</ol>
<p><strong>后端层面：</strong></p>
<ol>
<li class="lvl-3">
<p><strong>Token机制</strong>：每次请求验证唯一token</p>
</li>
<li class="lvl-3">
<p><strong>幂等性设计</strong>：相同请求只处理一次</p>
</li>
<li class="lvl-3">
<p><strong>请求去重</strong>：基于关键参数识别重复请求</p>
</li>
</ol>
<p><strong>重点：</strong> 前后端配合，既要防止技术层面的重复提交，也要保证业务逻辑的幂等性。</p>
<h3 id="前端防重复提交方案">前端防重复提交方案</h3>
<h4 id="方案一：按钮禁用（推荐）">方案一：按钮禁用（推荐）</h4>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;form @submit.prevent=&quot;handleSubmit&quot;&gt;</span><br><span class="line">    &lt;input v-model=&quot;formData.name&quot; required&gt;</span><br><span class="line">    &lt;button </span><br><span class="line">      type=&quot;submit&quot; </span><br><span class="line">      :disabled=&quot;submitting&quot;</span><br><span class="line">      :class=&quot;&#123; &#x27;loading&#x27;: submitting &#125;&quot;</span><br><span class="line">    &gt;</span><br><span class="line">      &#123;&#123; submitting ? &#x27;提交中...&#x27; : &#x27;提交&#x27; &#125;&#125;</span><br><span class="line">    &lt;/button&gt;</span><br><span class="line">  &lt;/form&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref, reactive &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">const formData = reactive(&#123;</span><br><span class="line">  name: &#x27;&#x27;,</span><br><span class="line">  email: &#x27;&#x27;</span><br><span class="line">&#125;)</span><br><span class="line">const submitting = ref(false)</span><br><span class="line"></span><br><span class="line">const handleSubmit = async () =&gt; &#123;</span><br><span class="line">  if (submitting.value) return</span><br><span class="line">  </span><br><span class="line">  submitting.value = true</span><br><span class="line">  try &#123;</span><br><span class="line">    // 模拟 API 调用</span><br><span class="line">    await submitForm(formData)</span><br><span class="line">    alert(&#x27;提交成功！&#x27;)</span><br><span class="line">    // 重置表单</span><br><span class="line">    Object.assign(formData, &#123; name: &#x27;&#x27;, email: &#x27;&#x27; &#125;)</span><br><span class="line">  &#125; catch (error) &#123;</span><br><span class="line">    console.error(&#x27;提交失败:&#x27;, error)</span><br><span class="line">    alert(&#x27;提交失败，请重试&#x27;)</span><br><span class="line">  &#125; finally &#123;</span><br><span class="line">    submitting.value = false</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const submitForm = (data) =&gt; &#123;</span><br><span class="line">  return new Promise((resolve) =&gt; &#123;</span><br><span class="line">    setTimeout(() =&gt; resolve(&#123; success: true &#125;), 2000)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;style scoped&gt;</span><br><span class="line">.loading &#123;</span><br><span class="line">  opacity: 0.6;</span><br><span class="line">  cursor: not-allowed;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br></pre></td></tr></table></figure>
<h4 id="方案二：防抖函数">方案二：防抖函数</h4>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref, reactive &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">const formData = reactive(&#123; name: &#x27;&#x27;, email: &#x27;&#x27; &#125;)</span><br><span class="line">const submitting = ref(false)</span><br><span class="line"></span><br><span class="line">// 防抖函数</span><br><span class="line">const debounce = (func, wait) =&gt; &#123;</span><br><span class="line">  let timeout</span><br><span class="line">  return function executedFunction(...args) &#123;</span><br><span class="line">    const later = () =&gt; &#123;</span><br><span class="line">      clearTimeout(timeout)</span><br><span class="line">      func(...args)</span><br><span class="line">    &#125;</span><br><span class="line">    clearTimeout(timeout)</span><br><span class="line">    timeout = setTimeout(later, wait)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const handleSubmit = debounce(async () =&gt; &#123;</span><br><span class="line">  if (submitting.value) return</span><br><span class="line">  </span><br><span class="line">  submitting.value = true</span><br><span class="line">  try &#123;</span><br><span class="line">    await submitForm(formData)</span><br><span class="line">    alert(&#x27;提交成功！&#x27;)</span><br><span class="line">  &#125; catch (error) &#123;</span><br><span class="line">    alert(&#x27;提交失败&#x27;)</span><br><span class="line">  &#125; finally &#123;</span><br><span class="line">    submitting.value = false</span><br><span class="line">  &#125;</span><br><span class="line">&#125;, 1000) // 1秒内只能提交一次</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>
<h4 id="方案三：全局状态管理">方案三：全局状态管理</h4>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// stores/submitStore.js</span></span><br><span class="line"><span class="keyword">import</span> &#123; defineStore &#125; <span class="keyword">from</span> <span class="string">&#x27;pinia&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> useSubmitStore = <span class="title function_">defineStore</span>(<span class="string">&#x27;submit&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">state</span>: <span class="function">() =&gt;</span> (&#123;</span><br><span class="line">    <span class="attr">submittingForms</span>: <span class="keyword">new</span> <span class="title class_">Set</span>()</span><br><span class="line">  &#125;),</span><br><span class="line">  </span><br><span class="line">  <span class="attr">actions</span>: &#123;</span><br><span class="line">    <span class="title function_">startSubmit</span>(<span class="params">formId</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">submittingForms</span>.<span class="title function_">has</span>(formId)) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;表单正在提交中&#x27;</span>)</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">submittingForms</span>.<span class="title function_">add</span>(formId)</span><br><span class="line">    &#125;,</span><br><span class="line">    </span><br><span class="line">    <span class="title function_">endSubmit</span>(<span class="params">formId</span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">submittingForms</span>.<span class="title function_">delete</span>(formId)</span><br><span class="line">    &#125;,</span><br><span class="line">    </span><br><span class="line">    <span class="title function_">isSubmitting</span>(<span class="params">formId</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">submittingForms</span>.<span class="title function_">has</span>(formId)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; useSubmitStore &#125; from &#x27;@/stores/submitStore&#x27;</span><br><span class="line"></span><br><span class="line">const submitStore = useSubmitStore()</span><br><span class="line">const formId = &#x27;user-registration&#x27;</span><br><span class="line"></span><br><span class="line">const handleSubmit = async () =&gt; &#123;</span><br><span class="line">  if (submitStore.isSubmitting(formId)) &#123;</span><br><span class="line">    alert(&#x27;请勿重复提交&#x27;)</span><br><span class="line">    return</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  try &#123;</span><br><span class="line">    submitStore.startSubmit(formId)</span><br><span class="line">    await submitForm(formData)</span><br><span class="line">    alert(&#x27;提交成功！&#x27;)</span><br><span class="line">  &#125; catch (error) &#123;</span><br><span class="line">    alert(&#x27;提交失败&#x27;)</span><br><span class="line">  &#125; finally &#123;</span><br><span class="line">    submitStore.endSubmit(formId)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>
<h2 id="自己实现一个带搜素的下拉框">自己实现一个带搜素的下拉框</h2>
<p><strong>功能设计：</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>输入框支持输入搜索</p>
</li>
<li class="lvl-2">
<p>下拉列表展示过滤结果</p>
</li>
<li class="lvl-2">
<p>键盘导航（上下箭头、回车选择）</p>
</li>
<li class="lvl-2">
<p>点击外部自动关闭</p>
</li>
</ul>
<p><strong>技术实现要点：</strong></p>
<ol>
<li class="lvl-3">
<p><strong>数据流</strong>：使用v-model实现双向绑定</p>
</li>
<li class="lvl-3">
<p><strong>搜索过滤</strong>：支持本地过滤和远程搜索两种模式</p>
</li>
<li class="lvl-3">
<p><strong>用户体验</strong>：防抖搜索、加载状态、空状态提示</p>
</li>
<li class="lvl-3">
<p><strong>交互优化</strong>：点击外部关闭、键盘快捷键、动画效果</p>
</li>
</ol>
<p><strong>组件设计：</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>合理的props设计（options、filterable、remote等）</p>
</li>
<li class="lvl-2">
<p>完整的事件体系（change、search等）</p>
</li>
<li class="lvl-2">
<p>支持插槽自定义选项渲染</p>
</li>
<li class="lvl-2">
<p>良好的可访问性支持</p>
</li>
</ul>
<p><strong>核心难点：</strong> 搜索性能优化和键盘交互的完整实现。</p>
<h3 id="完整实现">完整实现</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div class=&quot;custom-select&quot; ref=&quot;selectRef&quot;&gt;</span><br><span class="line">    &lt;!-- 输入框 --&gt;</span><br><span class="line">    &lt;div </span><br><span class="line">      class=&quot;select-trigger&quot;</span><br><span class="line">      :class=&quot;&#123; &#x27;is-open&#x27;: isOpen, &#x27;has-value&#x27;: selectedOption &#125;&quot;</span><br><span class="line">      @click=&quot;toggleDropdown&quot;</span><br><span class="line">    &gt;</span><br><span class="line">      &lt;input</span><br><span class="line">        ref=&quot;inputRef&quot;</span><br><span class="line">        type=&quot;text&quot;</span><br><span class="line">        v-model=&quot;searchText&quot;</span><br><span class="line">        :placeholder=&quot;selectedOption ? selectedOption.label : placeholder&quot;</span><br><span class="line">        @input=&quot;handleSearch&quot;</span><br><span class="line">        @keydown=&quot;handleKeydown&quot;</span><br><span class="line">        @focus=&quot;openDropdown&quot;</span><br><span class="line">      /&gt;</span><br><span class="line">      &lt;span class=&quot;select-arrow&quot;&gt;▼&lt;/span&gt;</span><br><span class="line">      &lt;span </span><br><span class="line">        v-if=&quot;clearable &amp;&amp; selectedOption&quot; </span><br><span class="line">        class=&quot;clear-btn&quot;</span><br><span class="line">        @click.stop=&quot;clearSelection&quot;</span><br><span class="line">      &gt;</span><br><span class="line">        ×</span><br><span class="line">      &lt;/span&gt;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 下拉菜单 --&gt;</span><br><span class="line">    &lt;div v-show=&quot;isOpen&quot; class=&quot;select-dropdown&quot;&gt;</span><br><span class="line">      &lt;!-- 搜索状态 --&gt;</span><br><span class="line">      &lt;div v-if=&quot;loading&quot; class=&quot;dropdown-loading&quot;&gt;</span><br><span class="line">        搜索中...</span><br><span class="line">      &lt;/div&gt;</span><br><span class="line">      </span><br><span class="line">      &lt;!-- 选项列表 --&gt;</span><br><span class="line">      &lt;div</span><br><span class="line">        v-for=&quot;(option, index) in filteredOptions&quot;</span><br><span class="line">        :key=&quot;option.value&quot;</span><br><span class="line">        class=&quot;dropdown-option&quot;</span><br><span class="line">        :class=&quot;&#123;</span><br><span class="line">          &#x27;selected&#x27;: isSelected(option),</span><br><span class="line">          &#x27;highlighted&#x27;: highlightedIndex === index</span><br><span class="line">        &#125;&quot;</span><br><span class="line">        @click=&quot;selectOption(option)&quot;</span><br><span class="line">        @mouseenter=&quot;highlightedIndex = index&quot;</span><br><span class="line">      &gt;</span><br><span class="line">        &lt;slot name=&quot;option&quot; :option=&quot;option&quot;&gt;</span><br><span class="line">          &lt;span class=&quot;option-label&quot;&gt;&#123;&#123; option.label &#125;&#125;&lt;/span&gt;</span><br><span class="line">        &lt;/slot&gt;</span><br><span class="line">        &lt;span v-if=&quot;isSelected(option)&quot; class=&quot;check-mark&quot;&gt;✓&lt;/span&gt;</span><br><span class="line">      &lt;/div&gt;</span><br><span class="line"></span><br><span class="line">      &lt;!-- 空状态 --&gt;</span><br><span class="line">      &lt;div v-if=&quot;!filteredOptions.length&quot; class=&quot;dropdown-empty&quot;&gt;</span><br><span class="line">        &#123;&#123; searchText ? &#x27;未找到匹配项&#x27; : &#x27;暂无数据&#x27; &#125;&#125;</span><br><span class="line">      &lt;/div&gt;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref, computed, onMounted, onUnmounted, nextTick &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">// Props</span><br><span class="line">const props = defineProps(&#123;</span><br><span class="line">  modelValue: &#123;</span><br><span class="line">    type: [String, Number, Object],</span><br><span class="line">    default: null</span><br><span class="line">  &#125;,</span><br><span class="line">  options: &#123;</span><br><span class="line">    type: Array,</span><br><span class="line">    required: true,</span><br><span class="line">    default: () =&gt; []</span><br><span class="line">  &#125;,</span><br><span class="line">  placeholder: &#123;</span><br><span class="line">    type: String,</span><br><span class="line">    default: &#x27;请选择...&#x27;</span><br><span class="line">  &#125;,</span><br><span class="line">  clearable: &#123;</span><br><span class="line">    type: Boolean,</span><br><span class="line">    default: false</span><br><span class="line">  &#125;,</span><br><span class="line">  filterable: &#123;</span><br><span class="line">    type: Boolean,</span><br><span class="line">    default: true</span><br><span class="line">  &#125;,</span><br><span class="line">  remote: &#123;</span><br><span class="line">    type: Boolean,</span><br><span class="line">    default: false</span><br><span class="line">  &#125;,</span><br><span class="line">  loading: &#123;</span><br><span class="line">    type: Boolean,</span><br><span class="line">    default: false</span><br><span class="line">  &#125;,</span><br><span class="line">  debounce: &#123;</span><br><span class="line">    type: Number,</span><br><span class="line">    default: 300</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">// Emits</span><br><span class="line">const emit = defineEmits([&#x27;update:modelValue&#x27;, &#x27;change&#x27;, &#x27;search&#x27;])</span><br><span class="line"></span><br><span class="line">// 响应式数据</span><br><span class="line">const isOpen = ref(false)</span><br><span class="line">const searchText = ref(&#x27;&#x27;)</span><br><span class="line">const highlightedIndex = ref(-1)</span><br><span class="line">const selectRef = ref(null)</span><br><span class="line">const inputRef = ref(null)</span><br><span class="line"></span><br><span class="line">// 计算属性</span><br><span class="line">const selectedOption = computed(() =&gt; &#123;</span><br><span class="line">  return props.options.find(opt =&gt; opt.value === props.modelValue) || null</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">const filteredOptions = computed(() =&gt; &#123;</span><br><span class="line">  if (!props.filterable || !searchText.value) &#123;</span><br><span class="line">    return props.options</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  const searchLower = searchText.value.toLowerCase()</span><br><span class="line">  return props.options.filter(option =&gt; </span><br><span class="line">    option.label.toLowerCase().includes(searchLower)</span><br><span class="line">  )</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">// 方法</span><br><span class="line">const isSelected = (option) =&gt; &#123;</span><br><span class="line">  return option.value === props.modelValue</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const toggleDropdown = () =&gt; &#123;</span><br><span class="line">  isOpen.value = !isOpen.value</span><br><span class="line">  if (isOpen.value) &#123;</span><br><span class="line">    highlightedIndex.value = -1</span><br><span class="line">    nextTick(() =&gt; &#123;</span><br><span class="line">      inputRef.value?.focus()</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const openDropdown = () =&gt; &#123;</span><br><span class="line">  isOpen.value = true</span><br><span class="line">  highlightedIndex.value = -1</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const closeDropdown = () =&gt; &#123;</span><br><span class="line">  isOpen.value = false</span><br><span class="line">  searchText.value = &#x27;&#x27;</span><br><span class="line">  highlightedIndex.value = -1</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const selectOption = (option) =&gt; &#123;</span><br><span class="line">  emit(&#x27;update:modelValue&#x27;, option.value)</span><br><span class="line">  emit(&#x27;change&#x27;, option)</span><br><span class="line">  closeDropdown()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const clearSelection = () =&gt; &#123;</span><br><span class="line">  emit(&#x27;update:modelValue&#x27;, null)</span><br><span class="line">  emit(&#x27;change&#x27;, null)</span><br><span class="line">  searchText.value = &#x27;&#x27;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 防抖搜索</span><br><span class="line">let searchTimer = null</span><br><span class="line">const handleSearch = () =&gt; &#123;</span><br><span class="line">  if (props.remote) &#123;</span><br><span class="line">    clearTimeout(searchTimer)</span><br><span class="line">    searchTimer = setTimeout(() =&gt; &#123;</span><br><span class="line">      emit(&#x27;search&#x27;, searchText.value)</span><br><span class="line">    &#125;, props.debounce)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 键盘导航</span><br><span class="line">const handleKeydown = (event) =&gt; &#123;</span><br><span class="line">  if (!isOpen.value) return</span><br><span class="line"></span><br><span class="line">  switch (event.key) &#123;</span><br><span class="line">    case &#x27;ArrowDown&#x27;:</span><br><span class="line">      event.preventDefault()</span><br><span class="line">      highlightedIndex.value = </span><br><span class="line">        highlightedIndex.value &gt;= filteredOptions.value.length - 1 </span><br><span class="line">          ? 0 </span><br><span class="line">          : highlightedIndex.value + 1</span><br><span class="line">      break</span><br><span class="line">      </span><br><span class="line">    case &#x27;ArrowUp&#x27;:</span><br><span class="line">      event.preventDefault()</span><br><span class="line">      highlightedIndex.value = </span><br><span class="line">        highlightedIndex.value &lt;= 0 </span><br><span class="line">          ? filteredOptions.value.length - 1 </span><br><span class="line">          : highlightedIndex.value - 1</span><br><span class="line">      break</span><br><span class="line">      </span><br><span class="line">    case &#x27;Enter&#x27;:</span><br><span class="line">      event.preventDefault()</span><br><span class="line">      if (highlightedIndex.value &gt;= 0 &amp;&amp; </span><br><span class="line">          filteredOptions.value[highlightedIndex.value]) &#123;</span><br><span class="line">        selectOption(filteredOptions.value[highlightedIndex.value])</span><br><span class="line">      &#125;</span><br><span class="line">      break</span><br><span class="line">      </span><br><span class="line">    case &#x27;Escape&#x27;:</span><br><span class="line">      closeDropdown()</span><br><span class="line">      break</span><br><span class="line">      </span><br><span class="line">    case &#x27;Tab&#x27;:</span><br><span class="line">      closeDropdown()</span><br><span class="line">      break</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 点击外部关闭</span><br><span class="line">const handleClickOutside = (event) =&gt; &#123;</span><br><span class="line">  if (selectRef.value &amp;&amp; !selectRef.value.contains(event.target)) &#123;</span><br><span class="line">    closeDropdown()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 生命周期</span><br><span class="line">onMounted(() =&gt; &#123;</span><br><span class="line">  document.addEventListener(&#x27;click&#x27;, handleClickOutside)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">onUnmounted(() =&gt; &#123;</span><br><span class="line">  document.removeEventListener(&#x27;click&#x27;, handleClickOutside)</span><br><span class="line">  clearTimeout(searchTimer)</span><br><span class="line">&#125;)</span><br><span class="line">&lt;/script&gt;</span><br><span class="line"></span><br><span class="line">&lt;style scoped&gt;</span><br><span class="line">.custom-select &#123;</span><br><span class="line">  position: relative;</span><br><span class="line">  width: 100%;</span><br><span class="line">  max-width: 300px;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger &#123;</span><br><span class="line">  position: relative;</span><br><span class="line">  border: 1px solid #dcdfe6;</span><br><span class="line">  border-radius: 4px;</span><br><span class="line">  padding: 8px 32px 8px 12px;</span><br><span class="line">  background: white;</span><br><span class="line">  cursor: pointer;</span><br><span class="line">  transition: border-color 0.3s;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger:hover &#123;</span><br><span class="line">  border-color: #c0c4cc;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger.is-open &#123;</span><br><span class="line">  border-color: #409eff;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger input &#123;</span><br><span class="line">  border: none;</span><br><span class="line">  outline: none;</span><br><span class="line">  width: 100%;</span><br><span class="line">  background: transparent;</span><br><span class="line">  cursor: pointer;</span><br><span class="line">  font-size: 14px;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger input:focus &#123;</span><br><span class="line">  cursor: text;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-arrow &#123;</span><br><span class="line">  position: absolute;</span><br><span class="line">  right: 12px;</span><br><span class="line">  top: 50%;</span><br><span class="line">  transform: translateY(-50%);</span><br><span class="line">  transition: transform 0.3s;</span><br><span class="line">  color: #c0c4cc;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-trigger.is-open .select-arrow &#123;</span><br><span class="line">  transform: translateY(-50%) rotate(180deg);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.clear-btn &#123;</span><br><span class="line">  position: absolute;</span><br><span class="line">  right: 30px;</span><br><span class="line">  top: 50%;</span><br><span class="line">  transform: translateY(-50%);</span><br><span class="line">  cursor: pointer;</span><br><span class="line">  color: #c0c4cc;</span><br><span class="line">  font-size: 16px;</span><br><span class="line">  width: 16px;</span><br><span class="line">  height: 16px;</span><br><span class="line">  display: flex;</span><br><span class="line">  align-items: center;</span><br><span class="line">  justify-content: center;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.clear-btn:hover &#123;</span><br><span class="line">  color: #909399;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.select-dropdown &#123;</span><br><span class="line">  position: absolute;</span><br><span class="line">  top: 100%;</span><br><span class="line">  left: 0;</span><br><span class="line">  right: 0;</span><br><span class="line">  max-height: 200px;</span><br><span class="line">  overflow-y: auto;</span><br><span class="line">  background: white;</span><br><span class="line">  border: 1px solid #e4e7ed;</span><br><span class="line">  border-radius: 4px;</span><br><span class="line">  box-shadow: 0 2px 12px 0 rgba(0, 0, 0, 0.1);</span><br><span class="line">  z-index: 1000;</span><br><span class="line">  margin-top: 4px;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.dropdown-option &#123;</span><br><span class="line">  padding: 8px 12px;</span><br><span class="line">  cursor: pointer;</span><br><span class="line">  display: flex;</span><br><span class="line">  justify-content: space-between;</span><br><span class="line">  align-items: center;</span><br><span class="line">  font-size: 14px;</span><br><span class="line">  transition: background-color 0.3s;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.dropdown-option:hover &#123;</span><br><span class="line">  background-color: #f5f7fa;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.dropdown-option.selected &#123;</span><br><span class="line">  color: #409eff;</span><br><span class="line">  background-color: #f0f9ff;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.dropdown-option.highlighted &#123;</span><br><span class="line">  background-color: #f5f7fa;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.dropdown-loading,</span><br><span class="line">.dropdown-empty &#123;</span><br><span class="line">  padding: 8px 12px;</span><br><span class="line">  color: #c0c4cc;</span><br><span class="line">  text-align: center;</span><br><span class="line">  font-size: 14px;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.check-mark &#123;</span><br><span class="line">  color: #409eff;</span><br><span class="line">  font-weight: bold;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br></pre></td></tr></table></figure>
<h3 id="使用示例">使用示例</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;h3&gt;选择的值: &#123;&#123; selectedValue &#125;&#125;&lt;/h3&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 基础使用 --&gt;</span><br><span class="line">    &lt;CustomSelect</span><br><span class="line">      v-model=&quot;selectedValue&quot;</span><br><span class="line">      :options=&quot;options&quot;</span><br><span class="line">      placeholder=&quot;请选择水果&quot;</span><br><span class="line">    /&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 可搜索 --&gt;</span><br><span class="line">    &lt;CustomSelect</span><br><span class="line">      v-model=&quot;selectedValue&quot;</span><br><span class="line">      :options=&quot;options&quot;</span><br><span class="line">      placeholder=&quot;搜索水果...&quot;</span><br><span class="line">      filterable</span><br><span class="line">      clearable</span><br><span class="line">    /&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 远程搜索 --&gt;</span><br><span class="line">    &lt;CustomSelect</span><br><span class="line">      v-model=&quot;selectedValue&quot;</span><br><span class="line">      :options=&quot;options&quot;</span><br><span class="line">      placeholder=&quot;远程搜索...&quot;</span><br><span class="line">      filterable</span><br><span class="line">      remote</span><br><span class="line">      :loading=&quot;loading&quot;</span><br><span class="line">      @search=&quot;handleRemoteSearch&quot;</span><br><span class="line">    /&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref &#125; from &#x27;vue&#x27;</span><br><span class="line"></span><br><span class="line">const selectedValue = ref(&#x27;&#x27;)</span><br><span class="line">const loading = ref(false)</span><br><span class="line">const options = ref([</span><br><span class="line">  &#123; label: &#x27;苹果&#x27;, value: &#x27;apple&#x27; &#125;,</span><br><span class="line">  &#123; label: &#x27;香蕉&#x27;, value: &#x27;banana&#x27; &#125;,</span><br><span class="line">  &#123; label: &#x27;橙子&#x27;, value: &#x27;orange&#x27; &#125;,</span><br><span class="line">  &#123; label: &#x27;葡萄&#x27;, value: &#x27;grape&#x27; &#125;,</span><br><span class="line">  &#123; label: &#x27;西瓜&#x27;, value: &#x27;watermelon&#x27; &#125;</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line">const handleRemoteSearch = async (query) =&gt; &#123;</span><br><span class="line">  loading.value = true</span><br><span class="line">  // 模拟远程搜索</span><br><span class="line">  setTimeout(() =&gt; &#123;</span><br><span class="line">    options.value = [</span><br><span class="line">      &#123; label: `搜索结果: $&#123;query&#125;`, value: &#x27;search-result&#x27; &#125;,</span><br><span class="line">      &#123; label: &#x27;其他选项&#x27;, value: &#x27;other&#x27; &#125;</span><br><span class="line">    ]</span><br><span class="line">    loading.value = false</span><br><span class="line">  &#125;, 1000)</span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>
<p>这个自定义下拉框组件具有以下特性：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>支持 <code>v-model</code> 双向绑定</p>
</li>
<li class="lvl-2">
<p>本地搜索和远程搜索</p>
</li>
<li class="lvl-2">
<p>键盘导航支持</p>
</li>
<li class="lvl-2">
<p>点击外部关闭</p>
</li>
<li class="lvl-2">
<p>清空选项</p>
</li>
<li class="lvl-2">
<p>加载状态</p>
</li>
<li class="lvl-2">
<p>自定义选项插槽</p>
</li>
<li class="lvl-2">
<p>防抖搜索</p>
</li>
</ul>
]]></content>
      <categories>
        <category>面经</category>
        <category>2025.11.07面试</category>
      </categories>
  </entry>
  <entry>
    <title>RN优化</title>
    <url>/2024/10/13/RN%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="移除不必要权限">移除不必要权限</h2>
<p>例如，在<code>\[项目名]\android\app\src\main\</code>下<code>AndroidManifest.xml</code>中写入如下代码</p>
<span id="more"></span>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 移除读取电话状态的权限 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">tools:node</span>=<span class="string">&quot;remove&quot;</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.READ_PHONE_STATE&quot;</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 移除写入外部存储的权限。 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">tools:node</span>=<span class="string">&quot;remove&quot;</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 移除读取外部存储的权限 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">tools:node</span>=<span class="string">&quot;remove&quot;</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.READ_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure>
<!-- more -->
<p>我们还需要在<code>AndroidManifest.xml</code>中声明了一个名为tools的XML命名空间<code>xmlns:tools=&quot;http://schemas.android.com/tools&quot;</code></p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&lt;manifest xmlns:<span class="attribute">android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span> xmlns:<span class="attribute">tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span><br></pre></td></tr></table></figure>
<p><strong>移除不必要权限的好处（文心一言生成）</strong>：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>提升用户隐私保护</strong>：减少不必要的权限请求可以增强用户信任，因为用户越来越关注个人数据的安全与隐私。特别是读取电话状态、读写外部存储这样的敏感权限，移除后表明应用不会侵犯用户的通信记录或存储的数据。</p>
</li>
<li class="lvl-2">
<p><strong>简化权限管理</strong>：对于用户而言，应用程序请求的权限越少，他们在安装和使用过程中遇到的权限提示就越少，从而提升用户体验。用户不再需要为不相关的功能授予权限。</p>
</li>
<li class="lvl-2">
<p><strong>降低潜在的安全风险</strong>：每个权限都可能成为安全漏洞的入口。移除不必要的权限可以减少应用受到攻击的面，提高应用的整体安全性。</p>
</li>
<li class="lvl-2">
<p><strong>合规性增强</strong>：遵循更严格的隐私法规要求，比如GDPR（欧盟通用数据保护条例）或中国的《个人信息保护法》，这些法律强调最小必要原则，即只收集和处理完成业务所必需的最少个人信息。移除多余权限有助于企业符合此类法规要求。</p>
</li>
<li class="lvl-2">
<p><strong>优化应用审核过程</strong>：在Google Play Store或其他应用市场上架时，应用的权限使用是审核的一部分。减少非必要的权限可以简化审核流程，加快应用上架速度。</p>
</li>
<li class="lvl-2">
<p><strong>可能提升性能</strong>：虽然直接关联不大，但理论上减少对存储或硬件资源的访问需求，可能轻微提升应用运行效率，尤其是对于资源紧张的设备。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>APP</category>
        <category>React Native</category>
      </categories>
      <tags>
        <tag>React Native</tag>
      </tags>
  </entry>
  <entry>
    <title>RN问题解决</title>
    <url>/2024/10/13/RN%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p>2024.6.14时最新版本为 0.74.2，2024.8.1时最新版本为0.74.4，但其相关依赖不太稳定，尝试使用 0.74.2 版本操作</p>
<span id="more"></span>
<h2 id="创建项目时报错：RN-error-Error">创建项目时报错：<strong>RN error Error</strong></h2>
<p><strong><font style="color:#DF2A3F;">RN error Error: getaddrinfo ENOENT <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a> at GetAddrInfoReqWrap.onlookup [as oncomplete] (node:dns:108:26)</font></strong></p>
<blockquote>
<p><strong><font style="color:#C75C00;">raw.githubusercontent.com</font></strong> 是 GitHub 提供的一个服务，用于直接访问存储在 GitHub 仓库中的原始文件（如文本文件、脚本等），而无需下载整个仓库。在 React Native (RN) 开发环境中，这个 URL 常见于以下几种场景（文心一言生成，可了解）：</p>
</blockquote>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>依赖管理工具安装</strong>: 如 Homebrew 安装脚本 ruby -e “$(curl -fsSL <a href="https://raw.githubusercontent.com/Homebrew/install/master/install">https://raw.githubusercontent.com/Homebrew/install/master/install</a>)”，Homebrew 是一个 macOS 下的包管理器，广泛用于安装 RN 开发所需的工具链和依赖，比如 Node.js、watchman、Yarn 或者其他 CLI 工具。</p>
</li>
<li class="lvl-2">
<p><strong>脚本或配置文件获取</strong>: 在某些情况下，开发者可能会从 <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a> 直接拉取脚本或配置文件到本地，作为项目初始化的一部分或者是运行某些自动化任务的需要。</p>
</li>
<li class="lvl-2">
<p><strong>快速分享和集成代码片段</strong>: 开发者可能会分享一些示例代码或者配置文件的链接，其他用户可以直接通过这些链接查看或下载内容，便于快速集成或参考。</p>
</li>
<li class="lvl-2">
<p><strong>持续集成/部署脚本</strong>: 在 CI/CD 流程中，可能会使用此类链接来动态获取最新的脚本或配置，确保构建过程使用的是最新或指定版本的资源。</p>
</li>
</ul>
<p>其最显著的是我们在启动项目时，会自动将模拟器启动</p>
<p><strong>问题分析</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p><font style="color:rgb(38, 38, 38);">为无法访问该域名的问题</font></p>
</li>
</ul>
<p><strong>解决方案</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>检查网络设置</p>
</li>
<li class="lvl-2">
<p>使用代理服务</p>
</li>
<li class="lvl-2">
<p>更换 DNS 服务器等方法来解决访问限制</p>
</li>
</ul>
<p>DNS 服务可能是 <strong>域名</strong> 与 <strong>ip</strong> 未绑定的原因</p>
<p>我们需要查看 <font style="color:#C75C00;background-color:rgb(249, 242, 244);">C:\Windows\System32\drivers\etc</font><code>里查看是否有</code><font style="color:rgb(199, 92, 0);">raw.githubusercontent.com</font> 与其对应的 <strong>ip</strong> 绑定，没有的话我们需要去查看 <font style="color:rgb(199, 92, 0);">raw.githubusercontent.com</font> 所对应的 <strong>ip</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>进入该网址 <a href="https://www.ipaddress.com/">https://www.ipaddress.com/</a>，输入域名搜索<img data-src="/2024/10/13/RN%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3/image.png" class="" title="RN问题解决"></p>
</li>
<li class="lvl-2">
<p>下图所框便是其对应的 <strong>ip</strong><img data-src="/2024/10/13/RN%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3/image-1.png" class="" title="RN问题解决"></p>
</li>
<li class="lvl-2">
<p>在<font style="color:#C75C00;">C:\Windows\System32\drivers\etc</font> 里的 <font style="color:#C75C00;">hosts</font> 里最下面写入对应的配置，保存后重新创建项目</p>
</li>
</ul>
<blockquote>
<p>185.199.108.133 <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a></p>
<p>185.199.109.133 <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a></p>
<p>185.199.110.133 <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a></p>
<p>185.199.111.133 <a href="http://raw.githubusercontent.com">raw.githubusercontent.com</a></p>
</blockquote>
<h2 id="下载-gradle-x-x-all-zip-太慢">下载 <strong>gradle-x.x-all.zip 太慢</strong></h2>
<blockquote>
<p>Downloading <a href="https://services.gradle.org/distributions/gradle-8.6-all.zip">https://services.gradle.org/distributions/gradle-8.6-all.zip</a></p>
</blockquote>
<p><strong><font style="color:#000000;">解决方案</font></strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>配置镜像</p>
</li>
<li class="lvl-2">
<p>在 <font style="color:#C75C00;">[项目名]\android\gradle\wrapper\gradle-wrapper.properties</font> 中进行如下操作</p>
</li>
</ul>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line">distributionBase=GRADLE_USER_HOME</span><br><span class="line">distributionPath=<span class="keyword">wrapper</span>/dists</span><br><span class="line"># 原网站下载</span><br><span class="line"># distributionUrl=https\://services.gradle.org/distributions/gradle<span class="number">-8.6</span>-<span class="keyword">all</span>.zip</span><br><span class="line"># 配置阿里镜像下载</span><br><span class="line">distributionUrl=https\://mirrors.aliyun.com/macports/distfiles/gradle/gradle<span class="number">-8.6</span>-<span class="keyword">all</span>.zip</span><br><span class="line"></span><br><span class="line">networkTimeout=<span class="number">10000</span></span><br><span class="line">validateDistributionUrl=<span class="keyword">true</span></span><br><span class="line">zipStoreBase=GRADLE_USER_HOME</span><br><span class="line">zipStorePath=<span class="keyword">wrapper</span>/dists</span><br></pre></td></tr></table></figure>
<p>配置好之后仍满可以试试多次重新执行启动项目命令，当进度明显快了就可以了，第一次可能有点慢，因为要安装 <font style="color:#C75C00;">gradle-8.6-all.zip</font></p>
<h2 id="依赖库下载太慢">依赖库下载太慢</h2>
<p><strong>解决方案</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p><font style="color:#C75C00;">[项目名]\android\build.gradle</font> 中 <font style="color:#C75C00;background-color:rgb(250, 250, 250);">repositories</font> <font style="color:rgb(38, 38, 38);background-color:rgb(250, 250, 250);">内有</font> <font style="color:#C75C00;background-color:rgb(250, 250, 250);">google()</font> <font style="color:rgb(38, 38, 38);background-color:rgb(250, 250, 250);">和</font> <font style="color:#C75C00;background-color:rgb(250, 250, 250);">mavenCentral()</font></p>
</li>
</ul>
<figure class="highlight isbl"><table><tr><td class="code"><pre><span class="line"><span class="variable">repositories</span> &#123;</span><br><span class="line">    <span class="function"><span class="title">google</span>()</span></span><br><span class="line">    <span class="function"><span class="title">mavenCentral</span>()</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>google()</strong>: 这一行指定了Google的Maven仓库作为依赖项的来源。这对于下载Android SDK相关依赖、Jetpack库以及其他Google提供的库非常关键。</p>
</li>
<li class="lvl-2">
<p><strong>mavenCentral()</strong>: 这一行指定了Maven中央仓库，它是Java开发中最常用的依赖库仓库，包含了大量开源库和框架。</p>
</li>
<li class="lvl-2">
<p>但由于这两个仓库的服务器都在国外，我们也需要对其进行镜像处理，有两种解决方案：</p>
<ul class="lvl-2">
<li class="lvl-6"><strong>局部配置</strong>：直接在 <font style="color:#DF2A3F;">build.gradle</font> 的 <font style="color:#DF2A3F;background-color:rgb(250, 250, 250);">repositories</font> <font style="color:rgb(38, 38, 38);background-color:rgb(250, 250, 250);">中修改</font></li>
<li class="lvl-6"><strong>全局配置</strong>： 在 <font style="color:#DF2A3F;">C:\Users[用户名].gradle</font> 文件夹下创建 <font style="color:#DF2A3F;">init.gradle</font> 文件，里面进行如下配置，这样我们就不需要每个项目都进行配置了</li>
</ul>
</li>
</ul>
<figure class="highlight fsharp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 局部配置</span></span><br><span class="line"><span class="keyword">repositories</span> &#123;</span><br><span class="line">    <span class="keyword">maven</span> &#123; url <span class="symbol">&#x27;https</span><span class="operator">:</span><span class="comment">//maven.aliyun.com/repository/google&#x27; &#125;</span></span><br><span class="line">    <span class="keyword">maven</span> &#123; url <span class="symbol">&#x27;https</span><span class="operator">:</span><span class="comment">//maven.aliyun.com/repository/jcenter&#x27; &#125;</span></span><br><span class="line">    <span class="comment">// 此为阿里镜像下载源</span></span><br><span class="line">    <span class="keyword">maven</span> &#123; url <span class="symbol">&#x27;https</span><span class="operator">:</span><span class="comment">//mirrors.aliyun.com/macports/distfiles/gradle/&#x27; &#125;  </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight roboconf"><table><tr><td class="code"><pre><span class="line">// 全局配置</span><br><span class="line">allprojects&#123;</span><br><span class="line">    <span class="attribute">repositories &#123;</span></span><br><span class="line"><span class="attribute">        def ALIYUN_REPOSITORY_URL = &#x27;https</span>://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/central/&#x27;</span><br><span class="line">        def ALIYUN_JCENTER_URL = &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/public/&#x27;</span><br><span class="line">        all &#123; ArtifactRepository repo -&gt;</span><br><span class="line">            if(repo instanceof MavenArtifactRepository)&#123;</span><br><span class="line">                def url = repo<span class="variable">.url</span><span class="variable">.toString</span>()</span><br><span class="line">                if (url<span class="variable">.startsWith</span>(&#x27;https://repo1<span class="variable">.maven</span><span class="variable">.org</span>/maven2&#x27;) || url<span class="variable">.startsWith</span>(&#x27;http://repo1<span class="variable">.maven</span><span class="variable">.org</span>/maven2&#x27;)) &#123;</span><br><span class="line">                    project<span class="variable">.logger</span><span class="variable">.lifecycle</span> &quot;Repository $&#123;repo<span class="variable">.url</span>&#125; replaced by $ALIYUN_REPOSITORY_URL.&quot;</span><br><span class="line">                    remove repo</span><br><span class="line">                &#125;</span><br><span class="line">                if (url<span class="variable">.startsWith</span>(&#x27;https://jcenter<span class="variable">.bintray</span><span class="variable">.com</span>/&#x27;) || url<span class="variable">.startsWith</span>(&#x27;http://jcenter<span class="variable">.bintray</span><span class="variable">.com</span>/&#x27;)) &#123;</span><br><span class="line">                    project<span class="variable">.logger</span><span class="variable">.lifecycle</span> &quot;Repository $&#123;repo<span class="variable">.url</span>&#125; replaced by $ALIYUN_JCENTER_URL.&quot;</span><br><span class="line">                    remove repo</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        maven &#123;</span><br><span class="line">            url ALIYUN_REPOSITORY_URL</span><br><span class="line">            url ALIYUN_JCENTER_URL</span><br><span class="line">            url &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/google/&#x27;</span><br><span class="line">            url &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/gradle-plugin/&#x27;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    buildscript&#123;</span><br><span class="line">        repositories &#123;</span><br><span class="line">            def ALIYUN_REPOSITORY_URL = &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/central/&#x27;</span><br><span class="line">            def ALIYUN_JCENTER_URL = &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/public/&#x27;</span><br><span class="line">            all &#123; ArtifactRepository repo -&gt;</span><br><span class="line">                if(repo instanceof MavenArtifactRepository)&#123;</span><br><span class="line">                    def url = repo<span class="variable">.url</span><span class="variable">.toString</span>()</span><br><span class="line">                    if (url<span class="variable">.startsWith</span>(&#x27;https://repo1<span class="variable">.maven</span><span class="variable">.org</span>/maven2&#x27;) || url<span class="variable">.startsWith</span>(&#x27;http://repo1<span class="variable">.maven</span><span class="variable">.org</span>/maven2&#x27;)) &#123;</span><br><span class="line">                        project<span class="variable">.logger</span><span class="variable">.lifecycle</span> &quot;Repository $&#123;repo<span class="variable">.url</span>&#125; replaced by $ALIYUN_REPOSITORY_URL.&quot;</span><br><span class="line">                        remove repo</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (url<span class="variable">.startsWith</span>(&#x27;https://jcenter<span class="variable">.bintray</span><span class="variable">.com</span>/&#x27;) || url<span class="variable">.startsWith</span>(&#x27;http://jcenter<span class="variable">.bintray</span><span class="variable">.com</span>/&#x27;)) &#123;</span><br><span class="line">                        project<span class="variable">.logger</span><span class="variable">.lifecycle</span> &quot;Repository $&#123;repo<span class="variable">.url</span>&#125; replaced by $ALIYUN_JCENTER_URL.&quot;</span><br><span class="line">                        remove repo</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            maven &#123;</span><br><span class="line">                url ALIYUN_REPOSITORY_URL</span><br><span class="line">                url ALIYUN_JCENTER_URL</span><br><span class="line">                url &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/google/&#x27;</span><br><span class="line">                url &#x27;https://maven<span class="variable">.aliyun</span><span class="variable">.com</span>/repository/gradle-plugin/&#x27;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>
<h2 id="调试断开后设备上打开报错">调试断开后设备上打开报错</h2>
<p>当我们启动程序并连上设备后，我们可以看到页面资源，但当我们断开后再打开就会报错 <font style="color:#DF2A3F;"><strong>Unable to load script…</strong></font>，是因为我们的 <strong>JS代码和静态资源</strong> 并没有集成到Android项目中进行构建和发布</p>
<p><strong>解决方案</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>在 <font style="color:#DF2A3F;">[项目名]\android\app\src\main\</font> 目录下创建 <font style="color:#DF2A3F;">assets</font> 文件夹，并在下面创建 <font style="color:#DF2A3F;">index.android.bundle</font> 文件</p>
</li>
<li class="lvl-2">
<p>然后在控制台<strong>项目根目录</strong>运行命令 <font style="color:#DF2A3F;">npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res</font></p>
</li>
<li class="lvl-2">
<p>当我们再次运行时，即使断开我们也可以看到我们的资源</p>
</li>
</ul>
<h2 id="error-Error-connect-ETIMEDOUT-185-199-108-133-443">error Error: connect ETIMEDOUT 185.199.108.133:443</h2>
<p>过段时间重新尝试安装</p>
<h2 id="error-unknown-command-‘run-android’">error: unknown command ‘run-android’</h2>
<p>依赖包删除重新 <code>npm i</code> 或 <code>yarn</code></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>APP</category>
        <category>React Native</category>
      </categories>
      <tags>
        <tag>React Native</tag>
      </tags>
  </entry>
  <entry>
    <title>SASS星空</title>
    <url>/2024/10/15/SASS%E6%98%9F%E7%A9%BA/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p><strong>效果图</strong></p>
<img data-src="/2024/10/15/SASS%E6%98%9F%E7%A9%BA/preview.gif" class="" title="SASS星空">
<p>盒子的阴影可以设置多个，所以我们可以通过阴影实现星光的效果，但纯写 css 过于抽象，我们可以利用 sass 的循环语法、函数与变量编写</p>
<p><strong>HTML结构</strong></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;./index.css&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 这里只设置三个层级,可以根据需要自己添加 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;layer1&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;layer2&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;layer3&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;title&quot;</span>&gt;</span>Sass 星空<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>SASS代码</strong><br>
提示：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>对SASS的编译不要用 VScode 的插件：easy sass，可能会出现函数识别导致编译失败的问题</p>
</li>
<li class="lvl-2">
<p>可以在 node 环境下，安装 sass：<code>npm i sass</code></p>
</li>
<li class="lvl-2">
<p>后执行 <code>npx sass [sass文件路径] [编译转换后的css输出文件路径] --no-source-map -w</code></p>
</li>
</ul>
<blockquote>
<p>npx sass index.scss index.css --no-source-map -w</p>
</blockquote>
<ul class="lvl-0">
<li class="lvl-2">
<p>npx：暂时把 sass 加入环境变量中，仅在此命令行操作有效</p>
</li>
<li class="lvl-2">
<p>sass：安装 sass 自带的执行命令</p>
</li>
<li class="lvl-2">
<p>–no-source-map：取消源映射，source-map里面存放着sass与css的映射信息，通过此命令可以取消该文件的生成</p>
</li>
<li class="lvl-2">
<p>-w：监听sass/scss的文件内容变化，自动更新生成的css</p>
</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">// 背景底色</span><br><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="comment">/*</span></span><br><span class="line"><span class="comment">    radial-gradient 是 CSS3 中用于创建径向渐变背景的属性。</span></span><br><span class="line"><span class="comment">    ellipse at bottom 指定了渐变形状为椭圆，并且椭圆的底部与元素的底部对齐。</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">  <span class="attribute">background</span>: <span class="built_in">radial-gradient</span>(ellipse at bottom, <span class="number">#1b2735</span> <span class="number">0%</span>, <span class="number">#090a0f</span> <span class="number">100%</span>);</span><br><span class="line">  <span class="attribute">overflow</span>: hidden;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.title</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: absolute;</span><br><span class="line">  <span class="attribute">top</span>: <span class="number">50%</span>;</span><br><span class="line">  <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">color</span>: <span class="number">#fff</span>;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">  <span class="attribute">font-family</span>: <span class="string">&quot;lato&quot;</span>, sans-serif;</span><br><span class="line">  <span class="attribute">font-weight</span>: <span class="number">300</span>;</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="number">50px</span>;</span><br><span class="line">  <span class="attribute">letter-spacing</span>: <span class="number">10px</span>;</span><br><span class="line">  <span class="attribute">margin-top</span>: -<span class="number">60px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>: <span class="number">10px</span>;</span><br><span class="line">  // 渐变背景，将文本背景设置为透明，并设置背景颜色为 <span class="selector-id">#38495a</span></span><br><span class="line">  // 即将文字展示为背景颜色</span><br><span class="line">  <span class="attribute">background</span>: <span class="built_in">linear-gradient</span>(white, <span class="number">#38495a</span>);</span><br><span class="line">  -webkit-<span class="attribute">background-clip</span>: text;</span><br><span class="line">  <span class="attribute">background-clip</span>: text;</span><br><span class="line">  <span class="attribute">color</span>: transparent;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 创建星光（阴影）函数</span><br><span class="line"><span class="keyword">@function</span> createShadow($n) &#123;</span><br><span class="line">  // 定义阴影变量  </span><br><span class="line">  $shadow: <span class="string">&quot;#&#123;random($limit: 100)&#125;vw #&#123;random($limit: 100)&#125;vh #fff&quot;</span>;</span><br><span class="line">  <span class="keyword">@for</span> $i from <span class="number">2</span> through $n &#123;</span><br><span class="line">    // 每次循环向 $shadow 中添加新的阴影</span><br><span class="line">    $shadow: <span class="string">&quot;#&#123;$shadow&#125;, #&#123;random($limit: 100)&#125;vw #&#123;random($limit: 100)&#125;vh #fff&quot;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  // unquote() 函数用于将字符串转换为 CSS 属性值，即去掉字符串</span><br><span class="line">  <span class="keyword">@return</span> unquote($shadow);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 定义星光（阴影）初始数量</span><br><span class="line">$count: <span class="number">1000</span>;</span><br><span class="line">// 定义动画初始时间</span><br><span class="line">$duration: <span class="number">400s</span>;</span><br><span class="line"><span class="keyword">@for</span> $i from <span class="number">1</span> through <span class="number">3</span> &#123;</span><br><span class="line">  // 每次循环时将数量和动画时间减半，以达到不同层级的动画效果</span><br><span class="line">  $count: <span class="built_in">floor</span>(<span class="built_in">calc</span>($count / <span class="number">2</span>));</span><br><span class="line">  $duration: <span class="built_in">floor</span>(<span class="built_in">calc</span>($duration / <span class="number">2</span>));</span><br><span class="line"></span><br><span class="line">  // 输出调试信息</span><br><span class="line">  <span class="keyword">@debug</span> <span class="string">&quot;count #&#123;$count&#125;&quot;</span>;</span><br><span class="line">  <span class="keyword">@debug</span> <span class="string">&quot;duration #&#123;$duration&#125;&quot;</span>;</span><br><span class="line">  // 创建层级的样式</span><br><span class="line">  <span class="selector-class">.layer</span>#&#123;$<span class="selector-tag">i</span>&#125; &#123;</span><br><span class="line">    $size: #&#123;$<span class="selector-tag">i</span>&#125;px;</span><br><span class="line">    <span class="attribute">position</span>: fixed;</span><br><span class="line">    <span class="attribute">width</span>: $size;</span><br><span class="line">    <span class="attribute">height</span>: $size;</span><br><span class="line">    <span class="attribute">border-radius</span>: <span class="number">50%</span>;</span><br><span class="line">    <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">background</span>: <span class="number">#f40</span>;</span><br><span class="line">    <span class="attribute">box-shadow</span>: <span class="built_in">createShadow</span>($count);</span><br><span class="line">    <span class="attribute">animation</span>: moveUp $duration linear infinite;</span><br><span class="line">    // 为了动画的流畅和无缝衔接，在 &amp;<span class="selector-pseudo">::after</span> 中设置相同的参数</span><br><span class="line">    &amp;<span class="selector-pseudo">::after</span> &#123;</span><br><span class="line">      <span class="attribute">content</span>: <span class="string">&quot;&quot;</span>;</span><br><span class="line">      <span class="attribute">position</span>: fixed;</span><br><span class="line">      <span class="attribute">top</span>: <span class="number">100vh</span>;</span><br><span class="line">      <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">      <span class="attribute">width</span>: inherit;</span><br><span class="line">      <span class="attribute">height</span>: inherit;</span><br><span class="line">      <span class="attribute">box-shadow</span>: inherit;</span><br><span class="line">      <span class="attribute">background</span>: inherit;</span><br><span class="line">      <span class="attribute">border-radius</span>: inherit;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 移动动画</span><br><span class="line"><span class="keyword">@keyframes</span> moveUp &#123;</span><br><span class="line">  <span class="number">100%</span> &#123;</span><br><span class="line">    <span class="attribute">transform</span>: <span class="built_in">translateY</span>(-<span class="number">100vh</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
        <category>页面</category>
      </categories>
      <tags>
        <tag>SASS</tag>
        <tag>CSS</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2024/10/18/hello-world/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>
<h2 id="Quick-Start">Quick Start</h2>
<h3 id="Create-a-new-post">Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server">Run server</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files">Generate static files</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites">Deploy to remote sites</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>immer中间件</title>
    <url>/2025/09/05/immer%E4%B8%AD%E9%97%B4%E4%BB%B6/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>immer中间件</h1>
<h2 id="immer好处">immer好处</h2>
<ul class="lvl-0">
<li class="lvl-2">
<p>遵循不可变数据范式，同时使用普通的 JavaScript 对象、数组、Sets 和 Maps。无需学习新的 API 或 “mutations patterns”！</p>
</li>
<li class="lvl-2">
<p>强类型，无基于字符串的路径选择器等</p>
</li>
<li class="lvl-2">
<p>开箱即用的结构共享</p>
</li>
<li class="lvl-2">
<p>开箱即用的对象冻结</p>
</li>
<li class="lvl-2">
<p>深度更新轻而易举</p>
</li>
<li class="lvl-2">
<p>样板代码减少。更少的噪音，更简洁的代码</p>
</li>
<li class="lvl-2">
<p>对 JSON 补丁的一流支持</p>
</li>
<li class="lvl-2">
<p>小：3KB gzip</p>
</li>
</ul>
<h2 id="react-TODO-Demo">react   TODO Demo</h2>
<p>优化前：勾选任一条item，所以的item都会被重新render</p>
<p>优化后：勾选哪条item，哪条item重新item</p>
<img data-src="/2025/09/05/immer%E4%B8%AD%E9%97%B4%E4%BB%B6/image-1.png" class="" title="优化后效果图">
<img data-src="/2025/09/05/immer%E4%B8%AD%E9%97%B4%E4%BB%B6/image-2.png" class="" title="immer使用">
<p>用了immer之后可以直接操作state里面的数据</p>
<img data-src="/2025/09/05/immer%E4%B8%AD%E9%97%B4%E4%BB%B6/image-3.png" class="" title="使用immer之后数据操作">
<p><strong>immer官网：<a href="https://immerjs.github.io/immer/zh-CN/">https://immerjs.github.io/immer/zh-CN/</a></strong></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>React</category>
        <category>immer中间件</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>js手写题</title>
    <url>/2025/11/21/js%E6%89%8B%E5%86%99%E9%A2%98/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h2 id="高级应用题">高级应用题</h2>
<h3 id="写一个函数实现防抖（debounce）">写一个函数实现防抖（debounce）</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 防抖函数</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">myDebounce</span>(<span class="params">fn,delay</span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> timer = <span class="literal">null</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">...args</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (timer) <span class="built_in">clearTimeout</span>(timer);</span><br><span class="line">    timer = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      fn.<span class="title function_">apply</span>(<span class="variable language_">this</span>,args)</span><br><span class="line">    &#125;, delay);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="如何实现一个简单的Promise？">如何实现一个简单的Promise？</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">mySimplePromise</span>&#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">exector</span>)&#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">state</span> = <span class="string">&#x27;pending&#x27;</span> <span class="comment">// 初始状态</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">value</span> = <span class="literal">undefined</span>  <span class="comment">// 存储结果值或失败原因</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">onresolvedCallbacks</span> = []  <span class="comment">// 成功的回调队列</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">onRejectedCallbacks</span> = []  <span class="comment">// 失败的回调队列</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">resolve</span> = (<span class="params">value</span>) =&gt;&#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span> == <span class="string">&#x27;pending&#x27;</span>) &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">state</span> = <span class="string">&#x27;resolved&#x27;</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">value</span> = value</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">onresolvedCallbacks</span>.<span class="title function_">forEach</span>(<span class="function"><span class="params">fn</span> =&gt;</span> <span class="title function_">fn</span>());</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">reject</span> = (<span class="params">reson</span>) =&gt;&#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span> == <span class="string">&#x27;pending&#x27;</span>) &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">state</span> = <span class="string">&#x27;rejected&#x27;</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">value</span> = reson</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">onRejectedCallbacks</span>.<span class="title function_">forEach</span>(<span class="function"><span class="params">fn</span> =&gt;</span> <span class="title function_">fn</span>());</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">exector</span>(resolve,reject)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">then</span>(<span class="params">onResolved,onRejected</span>)&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span> == <span class="string">&#x27;resolved&#x27;</span>) &#123;</span><br><span class="line">      <span class="title function_">resolve</span>(onResolved)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span> == <span class="string">&#x27;rejected&#x27;</span>) &#123;</span><br><span class="line">      <span class="title function_">reject</span>(onRejected)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span> == <span class="string">&#x27;pending&#x27;</span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">onresolvedCallbacks</span>.<span class="title function_">push</span>(<span class="function">() =&gt;</span> <span class="title function_">onResolved</span>(<span class="variable language_">this</span>.<span class="property">value</span>))</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">onRejectedCallbacks</span>.<span class="title function_">push</span>(<span class="function">() =&gt;</span> <span class="title function_">onRejected</span>(<span class="variable language_">this</span>.<span class="property">value</span>))</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> promise1 = <span class="keyword">new</span> <span class="title function_">myPromise</span>(<span class="function">(<span class="params">resolve,reject</span>) =&gt;</span>&#123;</span><br><span class="line">  <span class="title function_">resolve</span>(<span class="string">&#x27;成功&#x27;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> promise2 = <span class="keyword">new</span> <span class="title function_">myPromise</span>(<span class="function">(<span class="params">resolve,reject</span>) =&gt;</span>&#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">resolve</span>(<span class="string">&#x27;延迟成功&#x27;</span>)</span><br><span class="line">  &#125;, <span class="number">2000</span>);</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">promise1.<span class="title function_">then</span>(<span class="function"><span class="params">res</span> =&gt;</span>&#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(res);  <span class="comment">//立即输出‘成功’</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">promise2.<span class="title function_">then</span>(<span class="function"><span class="params">res</span> =&gt;</span>&#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(res);  <span class="comment">//延迟2s输出‘延迟成功’</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h3 id="写一个函数实现柯里化（curry）">写一个函数实现柯里化（curry）</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">myCurry</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> <span class="title function_">curried</span>(<span class="params">...args</span>) &#123;</span><br><span class="line">     <span class="comment">// 检查当前参数数量是否达到原函数需要的参数数量</span></span><br><span class="line">    <span class="keyword">if</span> (args.<span class="property">length</span> &gt;= fn.<span class="property">length</span>) &#123;</span><br><span class="line">      <span class="comment">// 参数足够，直接执行原函数</span></span><br><span class="line">      <span class="keyword">return</span> fn.<span class="title function_">apply</span>(<span class="variable language_">this</span>,args)</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">      <span class="comment">// 参数不足，返回一个新函数继续收集参数</span></span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">...args2</span>) &#123;</span><br><span class="line">        <span class="comment">// 递归调用 curried，合并已有参数和新参数</span></span><br><span class="line">        <span class="keyword">return</span> curried.<span class="title function_">apply</span>(<span class="variable language_">this</span>,args.<span class="title function_">concat</span>(args2))</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">add</span>(<span class="params">a,b,c</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> a+b+c</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> curriedAdd = <span class="title function_">myCurry</span>(add)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">curriedAdd</span>(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>));</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">curriedAdd</span>(<span class="number">1</span>,<span class="number">2</span>)(<span class="number">3</span>));</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">curriedAdd</span>(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>));</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>面经</category>
        <category>js手写题</category>
      </categories>
  </entry>
  <entry>
    <title>vite 环境变量配置文件</title>
    <url>/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p><strong>前提文档链接</strong></p>
<p>Vite 环境变量和模式文档：<a href="https://vitejs.cn/vite3-cn/guide/env-and-mode.html#env-files">https://vitejs.cn/vite3-cn/guide/env-and-mode.html#env-files</a></p>
<hr>
<span id="more"></span>
<h2 id="开发环境与配置文件">开发环境与配置文件</h2>
<p>在项目开发中，我们往往需要在不同环境下使用不同的配置，比如开发环境、测试环境、生产环境等。</p>
<p>在项目开发中，常见的环境有</p>
<ul class="lvl-0">
<li class="lvl-2">
<p><code>dev</code> – 开发环境，外部用户无法访问，开发人员使用，版本变动大。</p>
</li>
<li class="lvl-2">
<p><code>prod</code> – 生产环境，面向外部用户的环境，正式环境。</p>
</li>
<li class="lvl-2">
<p><code>test</code> – 测试环境，外部用户无法访问，专门给测试人员使用的，版本相对稳定。</p>
</li>
<li class="lvl-2">
<p><code>sit (System Integration Test)</code> – 集成环境，系统集成测试，开发人员自己测试流程是否走通。</p>
</li>
<li class="lvl-2">
<p><code>uat (User Acceptance Test environment)</code> – 用户验收测试环境，主要面向要交付的人员进行验证测试。</p>
</li>
<li class="lvl-2">
<p><code>pre</code> – 灰度环境，外部用户可以访问，但是服务器配置相对低，其它和生产一样。</p>
</li>
<li class="lvl-2">
<p><code>fat (Feature Acceptance Test environment)</code> – 功能验收测试环境，用于软件测试者测试使用。在以 <a href="https://vitejs.cn/">Vite</a> 作为构建工具开发项目时，环境变量配置文件名为 <code>.env</code>，文件名后缀为 <code>.env.[mode]</code>，其中 <code>mode</code> 为环境名称，如 <code>dev</code>、<code>prod</code>、<code>test</code> 等。</p>
</li>
</ul>
<p>根据 <code>mode</code> 的不同，可以配置不同的环境变量，例如</p>
<blockquote>
<p>.env.development<br>
.env.production<br>
.env.test</p>
</blockquote>
<p>默认情况下，Vite 会自动读取 <code>.env</code> 文件（通用配置），并根据 <code>NODE_ENV</code> 的值来确定当前环境。</p>
<p>但对于一份指定模式的文件（例如 .env.production）会比通用形式的优先级更高（例如 .env）。且相同的配置项，指定模式的文件会覆盖通用配置。</p>
<p>虽说环境有多种，但真正的开发模式只有两种</p>
<ul class="lvl-0">
<li class="lvl-2">
<p><code>development</code>：开发模式下</p>
</li>
<li class="lvl-2">
<p><code>production</code>：生产模式下</p>
</li>
</ul>
<p>我们可以在不同的开发模式下配置多种环境，基于这种情况下，又习惯性的把 <code>开发环境</code> 和 <code>生产环境</code> 作为主环境，而在主环境下配置各种子环境，显示的把开发模式指定出来，便于开发人员理解，基于此，有些项目的环境目录会出现以下情况</p>
<blockquote>
<p>.env.development<br>
.env.development.sit<br>
.env.production<br>
.env.production.uat</p>
</blockquote>
<h2 id="环境变量">环境变量</h2>
<p>在 Vite 中，环境变量的命名规则默认为 <code>VITE_</code> 开头，例如 <code>VITE_API_URL</code>。</p>
<p><strong>官方解释</strong></p>
<blockquote>
<p>为了防止意外地将一些环境变量泄漏到客户端，只有以 VITE_ 为前缀的变量才会暴露给经过 vite 处理的代码。例如下面这些环境变量：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">VITE_SOME_KEY</span>=<span class="number">123</span></span><br><span class="line"><span class="attr">DB_PASSWORD</span>=foobar</span><br></pre></td></tr></table></figure>
<p>只有 VITE_SOME_KEY 会被暴露为 import.meta.env.VITE_SOME_KEY 提供给客户端源码，而 DB_PASSWORD 则不会。</p>
<figure class="highlight arcade"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.<span class="built_in">log</span>(<span class="keyword">import</span>.meta.env.VITE_SOME_KEY) <span class="comment">// 123</span></span><br><span class="line"><span class="built_in">console</span>.<span class="built_in">log</span>(<span class="keyword">import</span>.meta.env.DB_PASSWORD) <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>
</blockquote>
<p>如果修改默认前缀，请参阅 <a href="https://vitejs.cn/vite3-cn/config/shared-options.html#envprefix">envprefix</a>。</p>
<h3 id="环境变量的使用">环境变量的使用</h3>
<p>在项目中，有些公共部分是需要我们抽离出来，作为环境变量进行配置，例如：<code>API_BASE_URL</code>，<code>ROUTE_BASE_URL</code>等等。</p>
<p>我们可以在 <code>.env.[mode]</code> 配置文件中填写</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">VITE_APP_API_URL</span>=http://x.x.x.x:xx</span><br><span class="line"><span class="attr">VITE_ROUTE_BASE_URL</span>=/xxx</span><br></pre></td></tr></table></figure>
<p>在项目中，我们可以通过 <code>import.meta.env.[变量名]</code> 来获取这些环境变量。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">VITE_APP_API_URL</span>)</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">import</span>.<span class="property">meta</span>.<span class="property">env</span>.<span class="property">VITE_ROUTE_BASE_URL</span>)</span><br></pre></td></tr></table></figure>
<h3 id="指定开发环境">指定开发环境</h3>
<p>我们可以通过获取这些环境变量以达到不同环境下使用不同变量的功能，但我们如何确定当前环境或者当前模式呢？</p>
<h4 id="指定开发环境的模式">指定开发环境的模式</h4>
<p>开发环境的模式需要在配置文件中配置 <code>NODE_ENV</code> 变量，Vite 会自动读取该变量，并根据该变量的值来确定当前开发模式，默认为 <code>development</code>。</p>
<p><code>NODE_ENV</code> 有两个值：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p><code>development</code>：开发模式</p>
</li>
<li class="lvl-2">
<p><code>production</code>：生产模式</p>
</li>
</ul>
<p>在有些时候，我们不需要进行显示的配置，Vite 内部会自动根据当前环境来确定开发模式。例如：</p>
<blockquote>
<p>默认情况下，开发服务器 (dev 命令) 运行在 development (开发) 模式，而 build 命令（打包命令）则运行在 production (生产) 模式。</p>
<p>这意味着当执行 vite build 时，它会自动加载 .env.production 中可能存在的环境变量。 ————<a href="https://vitejs.cn/vite3-cn/guide/env-and-mode.html#env-files">官方解释</a></p>
</blockquote>
<h4 id="指定环境类型">指定环境类型</h4>
<p>在上文我们提到了 <code>.env.[mode]</code> ，我们只需要改写 <code>mode</code> 值便可实现环境的创建，在真实情况下，<code>mode</code> 可以是任意值，但为了避免混淆，我们可以遵循以上的软规定。</p>
<p>通过改变 mode 的值便可创建一个环境，但除了 <code>.env.development</code> 和 .<code>env.production</code> 可以被 Vite 自动识别在 dev命令 和 build 命令情况下读取变量外，其他的 mode 配置只会根据 <code>NODE_ENV</code> 的值来识别其对应的配置文件。</p>
<p>我们还需要在启动命令中进行模式指定，在项目根目录的 <code>package.json</code> 中的执行脚本 <code>script</code> 中添加一条命令，如</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;dev-test&quot;</span><span class="punctuation">:</span> <span class="string">&quot;vite --mode test&quot;</span><span class="punctuation">,</span></span><br></pre></td></tr></table></figure>
<p>通过指定 <code>--mode</code> 参数，Vite 就会根据当前启动命令指定的模式来加载对应的 Vite 项目中 <code>.env.[mode]</code> mode 指定的配置文件。</p>
<h4 id="示例">示例</h4>
<p>在 <code>.env.development</code> 中配置</p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-2.png" class="" title="vite 环境变量配置文件">
<p>我在封装的请求模块中引用 <code>VITE_APP_API_URL</code> 变量</p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-1.png" class="" title="vite 环境变量配置文件">
<p>通过 <code>npm run dev</code> 启动项目，在网络请求中的 <code>baseURL</code> 显示为 <code>http://10.111.11.11:8080</code></p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-3.png" class="" title="vite 环境变量配置文件">
<p>我们更换环境</p>
<p>在 <code>.env.test</code> 中配置</p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-4.png" class="" title="vite 环境变量配置文件">
<p>在 <code>package.json</code> 中添加一条命令</p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-5.png" class="" title="vite 环境变量配置文件">
<p>运行 <code>npm run dev-test</code>，在网络请求中的 <code>baseURL</code> 显示为 <code>http://10.122.22.22:8001</code></p>
<img data-src="/2024/10/18/vite%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6/image-6.png" class="" title="vite 环境变量配置文件">
<h4 id="注意事项">注意事项</h4>
<p>当前 <code>Vite</code> 版本执行打包命令时，如果在 <code>.env,production</code> 中指定了 <code>NODE_ENV:production</code>，会报错，只需把该配置删除即可，打包时还是会读取 <code>.env,production</code> 的配置。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>工程化</category>
      </categories>
      <tags>
        <tag>vite</tag>
      </tags>
  </entry>
  <entry>
    <title>React 高阶组件、Render props、hooks 有什么区别</title>
    <url>/2025/09/23/React%20%E9%AB%98%E9%98%B6%E7%BB%84%E4%BB%B6%E3%80%81Render%20props%E3%80%81hooks%20%E6%9C%89%E4%BB%80%E4%B9%88%E5%8C%BA%E5%88%AB%EF%BC%8C%E4%B8%BA%E4%BB%80%E4%B9%88%E8%A6%81%E4%B8%8D%E6%96%AD%E8%BF%AD%E4%BB%A3/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h3 id="React-高阶组件、Render-props、hooks-有什么区别，为什么要不断迭代">React 高阶组件、Render props、hooks 有什么区别，为什么要不断迭代</h3>
<p>这三者是目前react解决代码复用的主要方式：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>高阶组件（HOC）是 React 中用于复用组件逻辑的一种高级技巧。HOC 自身不是 React API 的一部分，它是一种基于 React 的组合特性而形成的设计模式。具体而言，高阶组件是参数为组件，返回值为新组件的函数。</p>
</li>
<li class="lvl-2">
<p>render props是指一种在 React 组件之间使用一个值为函数的 prop 共享代码的简单技术，更具体的说，render prop 是一个用于告知组件需要渲染什么内容的函数 prop。</p>
</li>
<li class="lvl-2">
<p>通常，render props 和高阶组件只渲染一个子节点。让 Hook 来服务这个使用场景更加简单。这两种模式仍有用武之地，（例如，一个虚拟滚动条组件或许会有一个 renderltem 属性，或是一个可见的容器组件或许会有它自己的 DOM 结构）。但在大部分场景下，Hook 足够了，并且能够帮助减少嵌套。</p>
</li>
</ul>
<h4 id="HOC">HOC</h4>
<p>官方解释∶</p>
<p>高阶组件（HOC）是 React 中用于复用组件逻辑的一种高级技巧。HOC 自身不是 React API 的一部分，它是一种基于 React 的组合特性而形成的设计模式。</p>
<p>高阶组件（HOC）就是一个函数，且该函数接受一个组件作为参数，并返回一个新的组件，它只是一种组件的设计模式，这种设计模式是由react自身的组合性质必然产生的。我们将它们称为纯组件，因为它们可以接受任何动态提供的子组件，但它们不会修改或复制其输入组件中的任何行为。</p>
<p>简言之，HOC是一种组件的设计模式，HOC接受一个组件和额外的参数（如果需要），返回一个新的组件。HOC 是纯函数，没有副作用。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// hoc的定义</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">withSubscription</span>(<span class="params">WrappedComponent, selectData</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">class</span> <span class="title class_">extends</span> <span class="title class_">React</span>.<span class="property">Component</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">props</span>) &#123;</span><br><span class="line">      <span class="variable language_">super</span>(props);</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">state</span> = &#123;</span><br><span class="line">        <span class="attr">data</span>: <span class="title function_">selectData</span>(<span class="title class_">DataSource</span>, props)</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 一些通用的逻辑处理</span></span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="comment">// ... 并使用新数据渲染被包装的组件!</span></span><br><span class="line">      <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> <span class="attr">data</span>=<span class="string">&#123;this.state.data&#125;</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="keyword">const</span> <span class="title class_">BlogPostWithSubscription</span> = <span class="title function_">withSubscription</span>(<span class="title class_">BlogPost</span>,</span><br><span class="line">  <span class="function">(<span class="params">DataSource, props</span>) =&gt;</span> <span class="title class_">DataSource</span>.<span class="title function_">getBlogPost</span>(props.<span class="property">id</span>));</span><br></pre></td></tr></table></figure>
<p><strong>1）HOC的优缺点</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>优点∶ 逻辑服用、不影响被包裹组件的内部逻辑。</p>
</li>
<li class="lvl-2">
<p>缺点∶hoc传递给被包裹组件的props容易和被包裹后的组件重名，进而被覆盖</p>
</li>
</ul>
<p><strong>2）适用场景</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>代码复用，逻辑抽象</p>
</li>
<li class="lvl-2">
<p>渲染劫持</p>
</li>
<li class="lvl-2">
<p>State 抽象和更改</p>
</li>
<li class="lvl-2">
<p>Props 更改</p>
</li>
</ul>
<p><strong>3）具体应用例子</strong></p>
<ul class="lvl-0">
<li class="lvl-2">
<p>**权限控制：**利用高阶组件的 <strong>条件渲染</strong> 特性可以对页面进行权限控制，权限控制一般分为两个维度：页面级别和 页面元素级别</p>
</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// HOC.js</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">withAdminAuth</span>(<span class="params">WrappedComponent</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">class</span> <span class="title class_">extends</span> <span class="title class_">React</span>.<span class="property">Component</span> &#123;</span><br><span class="line">        state = &#123;</span><br><span class="line">            <span class="attr">isAdmin</span>: <span class="literal">false</span>,</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">async</span> <span class="title function_">UNSAFE_componentWillMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">const</span> currentRole = <span class="keyword">await</span> <span class="title function_">getCurrentUserRole</span>();</span><br><span class="line">            <span class="variable language_">this</span>.<span class="title function_">setState</span>(&#123;</span><br><span class="line">                <span class="attr">isAdmin</span>: currentRole === <span class="string">&#x27;Admin&#x27;</span>,</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">state</span>.<span class="property">isAdmin</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> (<span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>您没有权限查看该页面，请联系管理员！<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// pages/page-a.js</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PageA</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">props</span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>(props);</span><br><span class="line">        <span class="comment">// something here...</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="title function_">UNSAFE_componentWillMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="comment">// fetching data</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="comment">// render page with data</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withAdminAuth</span>(<span class="title class_">PageA</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// pages/page-b.js</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PageB</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">props</span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>(props);</span><br><span class="line">    <span class="comment">// something here...</span></span><br><span class="line">        &#125;</span><br><span class="line">    <span class="title function_">UNSAFE_componentWillMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">// fetching data</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">// render page with data</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withAdminAuth</span>(<span class="title class_">PageB</span>);</span><br></pre></td></tr></table></figure>
<ul class="lvl-0">
<li class="lvl-2">
<p>**组件渲染性能追踪：**借助父组件子组件生命周期规则捕获子组件的生命周期，可以方便的对某个组件的渲染时间进行记录∶</p>
</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Home</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">        <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> (<span class="language-xml"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>Hello World.<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">withTiming</span>(<span class="params">WrappedComponent</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">class</span> <span class="title class_">extends</span> <span class="title class_">WrappedComponent</span> &#123;</span><br><span class="line">            <span class="title function_">constructor</span>(<span class="params">props</span>) &#123;</span><br><span class="line">                <span class="variable language_">super</span>(props);</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">start</span> = <span class="number">0</span>;</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">end</span> = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_">UNSAFE_componentWillMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">                <span class="variable language_">super</span>.<span class="property">componentWillMount</span> &amp;&amp; <span class="variable language_">super</span>.<span class="title function_">componentWillMount</span>();</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">start</span> = <span class="title class_">Date</span>.<span class="title function_">now</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_">componentDidMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">                <span class="variable language_">super</span>.<span class="property">componentDidMount</span> &amp;&amp; <span class="variable language_">super</span>.<span class="title function_">componentDidMount</span>();</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">end</span> = <span class="title class_">Date</span>.<span class="title function_">now</span>();</span><br><span class="line">                <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`<span class="subst">$&#123;WrappedComponent.name&#125;</span> 组件渲染时间为 <span class="subst">$&#123;<span class="variable language_">this</span>.end - <span class="variable language_">this</span>.start&#125;</span> ms`</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="variable language_">super</span>.<span class="title function_">render</span>();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withTiming</span>(<span class="title class_">Home</span>);   </span><br></pre></td></tr></table></figure>
<p>注意：withTiming 是利用 反向继承 实现的一个高阶组件，功能是计算被包裹组件（这里是 Home 组件）的渲染时间。</p>
<ul class="lvl-0">
<li class="lvl-2">
<p><strong>页面复用</strong></p>
</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">withFetching</span> = fetching =&gt; <span class="function"><span class="params">WrappedComponent</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">class</span> <span class="title class_">extends</span> <span class="title class_">React</span>.<span class="property">Component</span> &#123;</span><br><span class="line">        state = &#123;</span><br><span class="line">            <span class="attr">data</span>: [],</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">async</span> <span class="title function_">UNSAFE_componentWillMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">const</span> data = <span class="keyword">await</span> <span class="title function_">fetching</span>();</span><br><span class="line">            <span class="variable language_">this</span>.<span class="title function_">setState</span>(&#123;</span><br><span class="line">                data,</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> <span class="attr">data</span>=<span class="string">&#123;this.state.data&#125;</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// pages/page-a.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withFetching</span>(<span class="title function_">fetching</span>(<span class="string">&#x27;science-fiction&#x27;</span>))(<span class="title class_">MovieList</span>);</span><br><span class="line"><span class="comment">// pages/page-b.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withFetching</span>(<span class="title function_">fetching</span>(<span class="string">&#x27;action&#x27;</span>))(<span class="title class_">MovieList</span>);</span><br><span class="line"><span class="comment">// pages/page-other.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">withFetching</span>(<span class="title function_">fetching</span>(<span class="string">&#x27;some-other-type&#x27;</span>))(<span class="title class_">MovieList</span>);</span><br></pre></td></tr></table></figure>
<h4 id="Render-props">Render props</h4>
<p>官方解释∶</p>
<p>&quot;render prop&quot;是指一种在 React 组件之间使用一个值为函数的 prop 共享代码的简单技术</p>
<p>具有render prop 的组件接受一个返回React元素的函数，将render的渲染逻辑注入到组件内部。在这里，&quot;render&quot;的命名可以是任何其他有效的标识符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// DataProvider组件内部的渲染逻辑如下</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DataProvider</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Components</span> &#123;</span><br><span class="line">     state = &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;Tom&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">          <span class="tag">&lt;<span class="name">p</span>&gt;</span>共享数据组件自己内部的渲染逻辑<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">          &#123; this.props.render(this.state) &#125;</span></span><br><span class="line"><span class="language-xml">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用方式</span></span><br><span class="line">&lt;<span class="title class_">DataProvider</span> render=&#123;<span class="function"><span class="params">data</span> =&gt;</span> (</span><br><span class="line">  <span class="language-xml"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>Hello &#123;data.name&#125;<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span></span><br><span class="line">)&#125;/&gt;</span><br></pre></td></tr></table></figure>
<p>由此可以看到，render props的优缺点也很明显∶</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>优点：数据共享、代码复用，将组件内的state作为props传递给调用者，将渲染逻辑交给调用者。</p>
</li>
<li class="lvl-2">
<p>缺点：无法在 return 语句外访问数据、嵌套写法不够优雅</p>
</li>
</ul>
<h4 id="Hooks">Hooks</h4>
<p>官方解释∶</p>
<p>Hook是 React 16.8 的新增特性。它可以让你在不编写 class 的情况下使用 state 以及其他的 React 特性。通过自定义hook，可以复用代码逻辑。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自定义一个获取订阅数据的hook</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">useSubscription</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> data = <span class="title class_">DataSource</span>.<span class="title function_">getComments</span>();</span><br><span class="line">  <span class="keyword">return</span> [data];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// </span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">CommentList</span>(<span class="params">props</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123;data&#125; = props;</span><br><span class="line">  <span class="keyword">const</span> [subData] = <span class="title function_">useSubscription</span>();</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">&lt;<span class="title class_">CommentList</span> data=<span class="string">&#x27;hello&#x27;</span> /&gt;</span><br></pre></td></tr></table></figure>
<p>以上可以看出，hook解决了hoc的prop覆盖的问题，同时使用的方式解决了render props的嵌套地狱的问题。hook的优点如下∶</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>使用直观；</p>
</li>
<li class="lvl-2">
<p>解决hoc的prop 重名问题；</p>
</li>
<li class="lvl-2">
<p>解决render props 因共享数据 而出现嵌套地狱的问题；</p>
</li>
<li class="lvl-2">
<p>能在return之外使用数据的问题。</p>
</li>
</ul>
<p>需要注意的是：hook只能在组件顶层使用，不可在分支语句中使用。</p>
<h4 id="总结">总结</h4>
<p>Hoc、render props和hook都是为了解决代码复用的问题，但是hoc和render props都有特定的使用场景和明显的缺点。hook是react16.8更新的新的API，让组件逻辑复用更简洁明了，同时也解决了hoc和render props的一些缺点。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>React</category>
        <category>React 高阶组件、Render props、hooks 有什么区别</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>Clipboard API</title>
    <url>/2025/07/19/Clipboard%20API/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>Clipboard API</h1>
<h2 id="基本介绍">基本介绍</h2>
<p>Clipboard API 是现代浏览器提供的 JavaScript API，允许网页以安全可控的方式访问系统剪贴板。它提供了以下核心功能：</p>
<ol>
<li class="lvl-3">
<p><strong>读取剪贴板内容</strong>（粘贴操作）</p>
</li>
<li class="lvl-3">
<p><strong>写入剪贴板内容</strong>（复制操作）</p>
</li>
<li class="lvl-3">
<p><strong>监听剪贴板事件</strong></p>
</li>
<li class="lvl-3">
<p><strong>处理多种数据类型</strong>（文本、HTML、图片等）</p>
</li>
</ol>
<h2 id="主要应用场景">主要应用场景</h2>
<ul class="lvl-0">
<li class="lvl-2">
<p>向网页上粘贴图片</p>
</li>
<li class="lvl-2">
<p>复制一些内容，当粘贴后变成另一段文字（比如：不让粘贴之类的文字）</p>
</li>
<li class="lvl-2">
<p>某些购物网站会读取粘贴板上的数据进行读取对应的商品</p>
</li>
</ul>
<h3 id="1-向网页上粘贴图片">1. 向网页上粘贴图片</h3>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;editor&quot;</span> <span class="attr">contenteditable</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">const</span> editor = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;.editor&#x27;</span>);</span></span><br><span class="line"><span class="language-javascript">    </span></span><br><span class="line"><span class="language-javascript">    editor.<span class="title function_">addEventListener</span>(<span class="string">&#x27;paste&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">if</span> (e.<span class="property">clipboardData</span>.<span class="property">files</span>.<span class="property">length</span> &gt; <span class="number">0</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">        e.<span class="title function_">preventDefault</span>();</span></span><br><span class="line"><span class="language-javascript">        <span class="keyword">const</span> file = e.<span class="property">clipboardData</span>.<span class="property">files</span>[<span class="number">0</span>];</span></span><br><span class="line"><span class="language-javascript">        <span class="keyword">const</span> img = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&#x27;img&#x27;</span>);</span></span><br><span class="line"><span class="language-javascript">        img.<span class="property">src</span> = <span class="variable constant_">URL</span>.<span class="title function_">createObjectURL</span>(file);</span></span><br><span class="line"><span class="language-javascript">        editor.<span class="title function_">appendChild</span>(img);</span></span><br><span class="line"><span class="language-javascript">      &#125;</span></span><br><span class="line"><span class="language-javascript">    &#125;);</span></span><br><span class="line"><span class="language-javascript">  </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>代码功能说明：</p>
<ol>
<li class="lvl-3">
<p>创建一个可编辑的div元素（<code>contenteditable</code>属性）</p>
</li>
<li class="lvl-3">
<p>监听粘贴事件，检查剪贴板中是否有文件</p>
</li>
<li class="lvl-3">
<p>如果检测到文件（如图片），则：</p>
<ul class="lvl-2">
<li class="lvl-5">阻止默认粘贴行为</li>
<li class="lvl-5">获取第一个文件</li>
<li class="lvl-5">创建img元素并设置src为文件对象URL</li>
<li class="lvl-5">将图片插入到编辑器中</li>
</ul>
</li>
</ol>
<h3 id="2-复制-粘贴内容转换">2. 复制/粘贴内容转换</h3>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 复制时转换内容</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;copy&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">  e.<span class="title function_">preventDefault</span>(); <span class="comment">// 阻止默认复制行为</span></span><br><span class="line">  navigator.<span class="property">clipboard</span>.<span class="title function_">weiteText</span>(<span class="string">&quot;不准复制，打钱！！！&quot;</span>)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h3 id="3-读取剪贴板数据">3. 读取剪贴板数据</h3>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;editor&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">        <span class="comment">// 修正后的剪贴板读取代码</span></span></span><br><span class="line"><span class="language-javascript">        navigator.<span class="property">clipboard</span>.<span class="title function_">readText</span>()</span></span><br><span class="line"><span class="language-javascript">            .<span class="title function_">then</span>(<span class="function">(<span class="params">text</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">                <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;.editor&#x27;</span>).<span class="property">innerHTML</span> = text;</span></span><br><span class="line"><span class="language-javascript">            &#125;)</span></span><br><span class="line"><span class="language-javascript">            .<span class="title function_">catch</span>(<span class="function">(<span class="params">error</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">                <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&#x27;读取剪贴板失败:&#x27;</span>, error);</span></span><br><span class="line"><span class="language-javascript">                <span class="title function_">alert</span>(<span class="string">&#x27;无法读取剪贴板内容&#x27;</span>);</span></span><br><span class="line"><span class="language-javascript">            &#125;);</span></span><br><span class="line"><span class="language-javascript">    </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="高级用法">高级用法</h2>
<h3 id="异步-Clipboard-API">异步 Clipboard API</h3>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 写入剪贴板</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">copyToClipboard</span>(<span class="params">text</span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">await</span> navigator.<span class="property">clipboard</span>.<span class="title function_">writeText</span>(text);</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;复制成功&#x27;</span>);</span><br><span class="line">  &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&#x27;复制失败:&#x27;</span>, err);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 读取剪贴板</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">pasteFromClipboard</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> text = <span class="keyword">await</span> navigator.<span class="property">clipboard</span>.<span class="title function_">readText</span>();</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;粘贴内容:&#x27;</span>, text);</span><br><span class="line">    <span class="keyword">return</span> text;</span><br><span class="line">  &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&#x27;读取剪贴板失败:&#x27;</span>, err);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="安全限制">安全限制</h2>
<ol>
<li class="lvl-3">
<p><strong>权限要求</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">写入操作通常不需要特殊权限</li>
<li class="lvl-5">读取操作需要用户明确授权（通过权限API请求<code>clipboard-read</code>权限）</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>用户交互要求</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">剪贴板访问必须在用户触发的事件（如点击）中执行</li>
<li class="lvl-5">不能在没有用户交互的情况下自动访问剪贴板</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>HTTPS要求</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">大多数浏览器要求页面通过HTTPS提供服务才能使用Clipboard API</li>
</ul>
</li>
</ol>
<h2 id="浏览器兼容性">浏览器兼容性</h2>
<ol>
<li class="lvl-3">
<p><strong>现代浏览器</strong>：Chrome、Edge、Firefox、Safari等主流浏览器均已支持</p>
</li>
<li class="lvl-3">
<p><strong>兼容性检测</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!navigator.<span class="property">clipboard</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;您的浏览器不支持Clipboard API&#x27;</span>);</span><br><span class="line">  <span class="comment">// 提供回退方案</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="实际应用建议">实际应用建议</h2>
<ol>
<li class="lvl-3">
<p><strong>提供回退方案</strong>：对于不支持Clipboard API的浏览器，可以使用<code>document.execCommand</code></p>
</li>
<li class="lvl-3">
<p><strong>清晰的用户提示</strong>：当需要读取剪贴板时，明确告知用户并请求权限</p>
</li>
<li class="lvl-3">
<p><strong>错误处理</strong>：妥善处理权限被拒绝的情况</p>
</li>
<li class="lvl-3">
<p><strong>性能考虑</strong>：处理大文件（如图片）时要注意内存使用</p>
</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>Clipboard API</category>
      </categories>
  </entry>
  <entry>
    <title>React事件机制</title>
    <url>/2025/09/23/React%E4%BA%8B%E4%BB%B6%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h3 id="React-事件机制">React 事件机制</h3>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;div onClick=&#123;<span class="variable language_">this</span>.<span class="property">handleClick</span>.<span class="title function_">bind</span>(<span class="variable language_">this</span>)&#125;&gt;点我&lt;/div&gt;</span><br></pre></td></tr></table></figure>
<p>​	React并不是将click事件绑定到了div的真实DOM上，而是在document处监听了所有的事件，当事件发生并且冒泡到document处的时候，React将事件内容封装并交由真正的处理函数运行。这样的方式不仅仅减少了内存的消耗，还能在组件挂在销毁时统一订阅和移除事件。</p>
<p>​	除此之外，冒泡到document上的事件也不是原生的浏览器事件，而是由react自己实现的合成事件（SyntheticEvent）。因此如果不想要是事件冒泡的话应该调用event.preventDefault()方法，而不是调用event.stopProppagation()方法。</p>
<img data-src="/2025/09/23/React%E4%BA%8B%E4%BB%B6%E6%9C%BA%E5%88%B6/image-1.png" class="" title="React事件机制图">
<p>​	JSX 上写的事件并没有绑定在对应的真实 DOM 上，而是通过事件代理的方式，将所有的事件都统一绑定在了 <code>document</code> 上。这样的方式不仅减少了内存消耗，还能在组件挂载销毁时统一订阅和移除事件。</p>
<p>​	另外冒泡到 <code>document</code> 上的事件也不是原生浏览器事件，而是 React 自己实现的合成事件（SyntheticEvent）。因此我们如果不想要事件冒泡的话，调用 <code>event.stopPropagation</code> 是无效的，而应该调用 <code>event.preventDefault</code>。</p>
<p>实现合成事件的目的如下：</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>合成事件首先抹平了浏览器之间的兼容问题，另外这是一个跨浏览器原生事件包装器，赋予了跨浏览器开发的能力；</p>
</li>
<li class="lvl-2">
<p>对于原生浏览器事件来说，浏览器会给监听器创建一个事件对象。如果你有很多的事件监听，那么就需要分配很多的事件对象，造成高额的内存分配问题。但是对于合成事件来说，有一个事件池专门来管理它们的创建和销毁，当事件需要被使用时，就会从池子中复用对象，事件回调结束后，就会销毁事件对象上的属性，从而便于下次复用事件对象。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>React</category>
        <category>React事件机制</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>创建RN项目</title>
    <url>/2024/10/13/%E5%88%9B%E5%BB%BARN%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h1>Android 开发环境</h1>
<ul class="lvl-0">
<li class="lvl-2">配置 Android 环境，请跟着<a href="https://reactnative.cn/docs/0.74/getting-started">文档</a>(0.74版本)进行</li>
</ul>
<span id="more"></span>
<h1>创建新项目</h1>
<ul class="lvl-0">
<li class="lvl-2">
<p>如果你之前全局安装过旧的react-native-cli命令行工具，请使用<code>npm uninstall -g react-native-cli</code>卸载掉它以避免一些冲突：<code>npm uninstall -g react-native-cli @react-native-community/cli</code></p>
</li>
<li class="lvl-2">
<p>输入以下命令创建新项目<code>npx react-native@latest init [项目名]</code></p>
</li>
</ul>
<h1>创建项目指定版本号：[可选参数] 指定版本或项目模板</h1>
<ul class="lvl-0">
<li class="lvl-2">
<p>你可以使用–version参数（注意是两个杠）创建指定版本的项目。注意版本号必须精确到两个小数点：<code>npx react-native@X.XX.X init AwesomeProject --version X.XX.X</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>APP</category>
        <category>React Native</category>
      </categories>
      <tags>
        <tag>React Native</tag>
      </tags>
  </entry>
  <entry>
    <title>前端面试题-类型判断（补充中）</title>
    <url>/2024/10/16/%E5%89%8D%E7%AB%AF%E9%9D%A2%E8%AF%95%E9%A2%98-%E7%B1%BB%E5%9E%8B%E5%88%A4%E6%96%AD/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="1-typeof-与-instanceof-类型判断">1. <code>typeof</code> 与 <code>instanceof</code> 类型判断</h2>
<details>
   <summary>
      <span>查看</span>
   </summary>
<blockquote>
<p><code>typeof</code> 是否能正确判断类型？<code>instanceof</code>能否正确判断对象的原理是什么？</p>
</blockquote>
<p><code>typeof</code> 对于原始类型来说，除了 <code>null</code> 都可以显示正确的类型。判断 <code>null</code> 对应的结果为 <code>object</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="number">1</span>); <span class="comment">// number</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="string">&quot;1&quot;</span>); <span class="comment">// string</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="literal">undefined</span>); <span class="comment">// undefined</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="literal">true</span>); <span class="comment">// boolean</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="title class_">Symbol</span>()); <span class="comment">// symbol</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="number">10n</span>); <span class="comment">// bigint</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="literal">null</span>); <span class="comment">// object</span></span><br></pre></td></tr></table></figure>
<img data-src="/2024/10/16/%E5%89%8D%E7%AB%AF%E9%9D%A2%E8%AF%95%E9%A2%98-%E7%B1%BB%E5%9E%8B%E5%88%A4%E6%96%AD/image.png" class="" title="typeof 与 instanceof 类型判断">
<p><code>typeof</code> 对于对象来说，除了函数都会显示 <code>object</code>，所以说 <code>typeof</code> 不能准确判断对象的类型。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> []); <span class="comment">// object</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> &#123;&#125;); <span class="comment">// object</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="keyword">function</span>(<span class="params"></span>)&#123;&#125;); <span class="comment">// function</span></span><br></pre></td></tr></table></figure>
<img data-src="/2024/10/16/%E5%89%8D%E7%AB%AF%E9%9D%A2%E8%AF%95%E9%A2%98-%E7%B1%BB%E5%9E%8B%E5%88%A4%E6%96%AD/image-1.png" class="" title="typeof 与 instanceof 类型判断">
<p>所以 <code>typeof</code> 不能正确判断类型。</p>
<blockquote>
<p>如果我们想判断一个对象的正确类型，我们可以考虑使用 <code>instanceof</code>，因为内部机制是通过原型链来判断的。但对于原始数据类型，使用 <code>instanceof</code> 是无法判断的。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(p1 <span class="keyword">instanceof</span> <span class="title class_">Person</span>); <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;hello world&#x27;</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(str <span class="keyword">instanceof</span> <span class="title class_">String</span>); <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
<img data-src="/2024/10/16/%E5%89%8D%E7%AB%AF%E9%9D%A2%E8%AF%95%E9%A2%98-%E7%B1%BB%E5%9E%8B%E5%88%A4%E6%96%AD/image-2.png" class="" title="typeof 与 instanceof 类型判断">
<div class="warning">
<p><strong>注意：</strong><br>
<code>instanceof</code> 可以判断对象的正确类型，但由于原型链的关系，对于其类型的父类进行判断也是返回 <code>true</code>。</p>
</div>
<h3 id="手动实现-function-isInstanceOf-child-Parent">手动实现 function isInstanceOf(child,Parent)</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">isInstanceof</span>(<span class="params">Child, Parent</span>) &#123;</span><br><span class="line">   <span class="comment">// 取右表达式的 prototype 值</span></span><br><span class="line">   <span class="keyword">let</span> <span class="title class_">ParentProto</span> = <span class="title class_">Parent</span>.<span class="property"><span class="keyword">prototype</span></span>;</span><br><span class="line">   <span class="comment">// 取左表达式的__proto__值</span></span><br><span class="line">   <span class="title class_">Child</span> = <span class="title class_">Child</span>.<span class="property">__proto__</span>;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">ParentProto</span>);</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">Child</span>);</span><br><span class="line">   <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">         <span class="keyword">if</span> (<span class="title class_">Child</span> === <span class="literal">null</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">         <span class="keyword">if</span> (<span class="title class_">Child</span> === <span class="title class_">ParentProto</span>) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">         <span class="title class_">Child</span> = <span class="title class_">Child</span>.<span class="property">__proto__</span>;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 开始测试</span></span><br><span class="line"><span class="keyword">var</span> a = []</span><br><span class="line"><span class="keyword">var</span> b = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Foo</span>(<span class="params"></span>) &#123; &#125;</span><br><span class="line"><span class="keyword">var</span> c = <span class="keyword">new</span> <span class="title class_">Foo</span>()</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">child</span>(<span class="params"></span>) &#123; &#125;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">father</span>(<span class="params"></span>) &#123; &#125;</span><br><span class="line">child.<span class="property"><span class="keyword">prototype</span></span> = <span class="keyword">new</span> <span class="title function_">father</span>()</span><br><span class="line"><span class="keyword">var</span> d = <span class="keyword">new</span> <span class="title function_">child</span>()</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(a, <span class="title class_">Array</span>)) <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(b, <span class="title class_">Object</span>)) <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(b, <span class="title class_">Array</span>)) <span class="comment">// false</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(a, <span class="title class_">Object</span>)) <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(c, <span class="title class_">Foo</span>)) <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(d, child)) <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">isInstanceof</span>(d, father)) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</details>
<h2 id="2-、-、Object-is-的区别">2. <code>==</code> 、<code>===</code> 、<code>Object.is()</code> 的区别</h2>
<h3 id=""><code>==</code></h3>
<details>
<summary>查看</summary>
相等（==）运算符（宽松比较）检查其两个操作数是否相等，返回一个布尔值结果。当比较双方数据类型不同时，它会尝试转换不同类型的操作数，并进行比较
<h4 id="比较规则"><code>==</code> 比较规则</h4>
<ol>
<li class="lvl-3">
<p>如果操作数具有相同的类型，则按如下方式进行比较：</p>
<ul class="lvl-2">
<li class="lvl-5">对象（Object）：仅当两个操作数引用同一个对象时返回 true。</li>
<li class="lvl-5">字符串（String）：仅当两个操作数具有相同的字符且顺序相同时返回 true。</li>
<li class="lvl-5">数字（Number）：如果两个操作数的值相同，则返回 true。+0 和 -0 被视为相同的值。如果任何一个操作数是 NaN，返回 false；所以，<mark>NaN 永远不等于 NaN</mark>。</li>
<li class="lvl-5">布尔值（Boolean）：仅当操作数都为 true 或都为 false 时返回 true。</li>
<li class="lvl-5">大整型（BigInt）：仅当两个操作数的值相同时返回 true。</li>
<li class="lvl-5">符号（Symbol）：仅当两个操作数引用相同的符号时返回 true。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="title class_">NaN</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="title class_">NaN</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a == b); <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
</li>
<li class="lvl-3">
<p>如果其中一个操作数为 null 或 undefined，另一个操作数也必须为 null 或 undefined 以返回 true。否则返回 false。（null 和 undefined 除了与null 和 undefined 比较为 true，其他值都返回 false）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="literal">null</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="literal">undefined</span>;</span><br><span class="line"><span class="keyword">let</span> c = <span class="literal">null</span>;</span><br><span class="line"><span class="keyword">let</span> d = <span class="literal">undefined</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a == b); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a == c); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(b == d); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a == d); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</li>
<li class="lvl-3">
<p>如果其中一个操作数是对象，另一个是原始值，则将对象转换为原始值。</p>
<ul class="lvl-2">
<li class="lvl-6">在这一步，两个操作数都被转换为原始值（字符串、数字、布尔值、符号和大整型中的一个）。剩余的转换将分情况完成。
<ul class="lvl-4">
<li class="lvl-8">如果是相同的类型，使用步骤 1 进行比较。</li>
<li class="lvl-8">如果其中一个操作数是符号(Symbol)而另一个不是，返回 false。</li>
<li class="lvl-8">如果其中一个操作数是布尔值而另一个不是，则将布尔值转换为数字：<mark>true 转换为 1，false 转换为 0</mark>。然后再次对两个操作数进行宽松比较。</li>
<li class="lvl-8">数字与字符串：<mark>将字符串转换为数字</mark>。转换失败将导致 NaN，这将保证相等比较为 false。</li>
<li class="lvl-8">数字与大整型：按数值进行比较。如果数字的值为 ±∞ 或 NaN，返回 false。</li>
<li class="lvl-8">字符串与大整型：<mark>使用与 BigInt() 构造函数相同的算法将字符串转换为大整型数</mark>。如果转换失败，返回 false。</li>
</ul>
</li>
</ul>
</li>
</ol>
<div class="tips">
<p><strong>对象转为原始值</strong><br>
对象将依次调用它的 <code>[Symbol.toPrimitive]()</code>（将 default 作为 hint 值）、valueOf() 和 toString() 方法，将其转换为原始值。<mark>注意，原始值转换会在 toString() 方法之前调用 valueOf() 方法</mark>。<br>
<code>[Symbol.toPrimitive]()</code> 方法，如果存在，则必须返回原始值，返回对象，会导致 TypeError。<mark>对于 valueOf() 和 toString()，如果其中一个返回对象，则忽略其返回值，从而使用另一个的返回值；如果两者都不存在，或者两者都没有返回一个原始值，则抛出 TypeError</mark>。</p>
</div>
<p><strong>示例</strong></p>
<p>定义一个干净的对象</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;&#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a); <span class="comment">// &#123;&#125;</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a[<span class="title class_">Symbol</span>.<span class="property">toPrimitive</span>]); <span class="comment">// undefined</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a.<span class="title function_">toString</span>()); <span class="comment">// [object Object]</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a.<span class="title function_">valueOf</span>()); <span class="comment">// &#123;&#125;</span></span><br></pre></td></tr></table></figure>
<p>实现该对象的<code>[Symbol.toPrimitive]</code>方法，使其返回一个原始值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">   [<span class="title class_">Symbol</span>.<span class="property">toPrimitive</span>](hint) &#123;</span><br><span class="line">         <span class="keyword">if</span> (hint === <span class="string">&#x27;number&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">42</span></span><br><span class="line">         &#125;<span class="keyword">else</span> <span class="keyword">if</span> (hint === <span class="string">&#x27;string&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;abc&#x27;</span></span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">   &#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 此处运用了隐式转换 导致使用强制数字类型转换算法 使得能满足 hint===&#x27;number&#x27; 条件        </span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(+a == <span class="number">42</span>); <span class="comment">// true</span></span><br><span class="line"><span class="comment">// 此处 new String 导致使用强制字符串类型转换算法 使得能满足 hint===&#x27;string&#x27; 条件 </span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">new</span> <span class="title class_">String</span>(a) == <span class="string">&#x27;abc&#x27;</span>); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a == <span class="literal">null</span>); <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
<div class="warning">
<p><strong>注意</strong><br>
当未对对象使用强制类型转换时，会返回默认值。但<code>Symbol.toPrimitive</code> 返回的 null 或 undefined 与 null 和 undefined 进行宽松比较(==)时仍为 false，hint 值是 number 时，返回的必须是数字字符串或数字，否则与数字或数字字符串进行宽松比较时仍为 false</p>
</div>
<div class="tips">
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Date">Date</a> 和 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Date">Symbol</a> 对象是唯一重写 <code>[Symbol.toPrimitive]()</code> 方法的对象。</p>
<p>对于 Date，如果 hint 是 “string” 或 “default”，<code>[Symbol.toPrimitive]()</code> 将会调用 toString。如果 toString 属性不存在，则调用 valueOf。如果 valueOf 也不存在，则抛出一个TypeError。</p>
<p>对于 Symbol，hint 参数未使用时，Symbol 的 <code>[Symbol.toPrimitive]()</code> 方法返回 Symbol 对象的原始值作为数据类型返回。</p>
</div>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> sym = <span class="title class_">Symbol</span>(<span class="string">&quot;example&quot;</span>);</span><br><span class="line">sym === sym[<span class="title class_">Symbol</span>.<span class="property">toPrimitive</span>](); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>当对象不存在<code>Symbol.toPrimitive</code>时，会根据 <code>toString()</code> 和 <code>valueOf()</code> 的返回值作为原始值进行比较。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">      <span class="title function_">valueOf</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">42</span></span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="title function_">toString</span>(<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;abc&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(a == <span class="number">42</span>); <span class="comment">// true</span></span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(a == <span class="string">&#x27;abc&#x27;</span>); <span class="comment">// false</span></span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">new</span> <span class="title class_">String</span>(a) == <span class="string">&#x27;abc&#x27;</span>); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<div class="tips">
<p>一般来说，valueOf 的优先级高于 toString。但当其使用强制字符串类型转换时，会根据 toString 的返回值作为原始值进行比较。</p>
</div>
</details>
<h3 id="-2"><code>===</code></h3>
<details>
<summary>查看</summary>
严格相等（===）运算符与 == 运算符之间最显著的区别是，严格相等运算符不尝试类型转换。相反，严格相等运算符总是认为不同类型的操作数是不同的，即只要类型不同进行 `===` 操作时，结果即为 `false`。严格相等运算符本质上只执行第 1 步，然后对所有其他情况返回 `false`。
<div class="warning">
<p>上面的算法有一个“故意违反”：如果其中一个操作数是 document.all，则它被视为 undefined。这意味着 document.all == null 是 true，但 document.all === undefined &amp;&amp; document.all === null 是 false。 —— <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Equality">MDN文章描述</a></p>
</div>
<h4 id="比较规则-2"><code>===</code> 比较规则</h4>
<ul class="lvl-0">
<li class="lvl-2">
<p>如果操作数的类型不同，则返回 false。</p>
</li>
<li class="lvl-2">
<p>如果两个操作数都是对象，只有当它们指向同一个对象时才返回 true。</p>
</li>
<li class="lvl-2">
<p>如果两个操作数都为 null，或者两个操作数都为 undefined，返回 true。</p>
</li>
<li class="lvl-2">
<p>如果两个操作数有任意一个为 NaN，返回 false。</p>
</li>
<li class="lvl-2">
<p>否则，比较两个操作数的值：</p>
<ul class="lvl-2">
<li class="lvl-4"><mark>数字类型必须拥有相同的数值。+0 和 -0 会被认为是相同的值。</mark></li>
<li class="lvl-4">字符串类型必须拥有相同顺序的相同字符。</li>
<li class="lvl-4">布尔运算符必须同时为 true 或同时为 false。</li>
</ul>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(-<span class="number">0</span> === +<span class="number">0</span>); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">0</span> === -<span class="number">0</span>); <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">NaN</span> === <span class="title class_">NaN</span>); <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
</details>
<h3 id="Object-is"><code>Object.is()</code></h3>
<details>
<summary>查看</summary>
<p><code>Object.is()</code> 静态方法确定两个值是否为相同值。</p>
<h4 id="Object-is-的比较规则"><code>Object.is()</code>的比较规则</h4>
<p>如果出现以下几种情况，则返回 true，即比较的两个值相同</p>
<ul class="lvl-0">
<li class="lvl-2">
<p>都是 undefined</p>
</li>
<li class="lvl-2">
<p>都是 null</p>
</li>
<li class="lvl-2">
<p>都是 true 或者都是 false</p>
</li>
<li class="lvl-2">
<p>都是长度相同、字符相同、顺序相同的字符串</p>
</li>
<li class="lvl-2">
<p>都是相同的对象（意味着两个值都引用了内存中的同一对象）</p>
</li>
<li class="lvl-2">
<p>都是 BigInt 且具有相同的数值</p>
</li>
<li class="lvl-2">
<p>都是 symbol 且引用相同的 symbol 值</p>
</li>
<li class="lvl-2">
<p>都是数字且</p>
<ul class="lvl-2">
<li class="lvl-4">都是 +0</li>
<li class="lvl-4">都是 -0</li>
<li class="lvl-4">都是 NaN</li>
<li class="lvl-4">都有相同的值，非零且都不是 NaN</li>
</ul>
</li>
</ul>
<p><code>Object.is()</code> 与 == 运算符并不等价。== 运算符在测试相等性之前，会对两个操作数进行类型转换（如果它们不是相同的类型），这可能会导致一些非预期的行为，例如 “” == false 的结果是 true，但是 <code>Object.is()</code> 不会对其操作数进行类型转换。</p>
<p><code>Object.is()</code> 也不等价于 === 运算符。<code>Object.is()</code> 和 === 之间的<mark>唯一区别在于它们处理带符号的 0 和 NaN 值的时候</mark>。=== 运算符（和 == 运算符）将数值 -0 和 +0 视为相等，但是会将 NaN 视为彼此不相等。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Object</span>.<span class="title function_">is</span>(<span class="number">0</span>, -<span class="number">0</span>); <span class="comment">// false</span></span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">is</span>(+<span class="number">0</span>, -<span class="number">0</span>); <span class="comment">// false</span></span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">is</span>(-<span class="number">0</span>, -<span class="number">0</span>); <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">is</span>(<span class="title class_">NaN</span>, <span class="number">0</span> / <span class="number">0</span>); <span class="comment">// true</span></span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">is</span>(<span class="title class_">NaN</span>, <span class="title class_">Number</span>.<span class="property">NaN</span>); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</details>
<h3 id="总结">总结</h3>
<div class="tips">
<p>对于 <code>==</code>，主要特点就是类型转换，而 <code>===</code> 和 <code>Object.is()</code> 则不进行类型转换。</p>
<p>对于 <code>===</code>，主要特点就是严格相等，类型不同就是 <code>false</code>，与 <code>==</code> 类型相同时比较规则一致。</p>
<p>对于 <code>Object.is()</code>，主要特点就是严格相等，类型不同就是 <code>false</code>，与 <code>==</code> 类型相同时比较规则一致。但与 <code>===</code> 不同的是，<code>Object.is()</code> 对于 0 和 -0、NaN 的处理不同，<code>Object.is()</code> 将数值 -0 和 +0 视为彼此不相等，但是会将 NaN 视为彼此相等，<code>===</code> 将数值 -0 和 +0 视为相等，但是会将 NaN 视为彼此不相等。</p>
</div>
<h2 id="全局-isNaN-和-Number-isNaN">全局 <code>isNaN()</code> 和  <code>Number.isNaN()</code></h2>
<details>
<summary>查看</summary>
<ul class="lvl-0">
<li class="lvl-2">
<p><code>isNaN()</code> 函数用来确定一个值是否为 NaN，若有必要，则首先将值转换为数字。</p>
</li>
<li class="lvl-2">
<p><code>Number.isNaN()</code> 静态方法判断传入的值是否为 NaN，如果输入不是数字类型，则返回 false。它是全局 <code>isNaN()</code> 函数更健壮的版本。</p>
</li>
</ul>
<p><code>isNaN()</code> 是全局对象的一个函数属性。对于数字值，<code>isNaN()</code> 检测该值是否为 NaN 值。当 <code>isNaN()</code> 函数的参数不是数字类型时，其会首先被转换为数字，然后将其结果值与 NaN 进行比较</p>
<p><code>isNaN()</code> 对于非数字参数的行为可能会令人困惑！例如，空字符串被强制转换为 0，布尔值被强制转换为 0 或 1；直观上，两者均“不是数字”，仅因它们的运算结果不是 NaN，而使得 <code>isNaN()</code> 返回 false。因此，<code>isNaN()</code> 既不回答“输入是否为浮点数值 NaN”，也不回答“输入是否为数字”这两个问题。</p>
<p>Number.isNaN() 是检测一个值是否为数字值 NaN 的更可靠的方法。或者，也可以使用表达式 x !== x，这两种方法都不会产生全局 isNaN() 不可靠的误判。要检测一个值是否为数字，请使用 typeof x === “number”。</p>
<h3 id="Number-isNaN-和全局-isNaN-之间的区别">Number.isNaN() 和全局 isNaN() 之间的区别</h3>
<p>Number.isNaN() 不会尝试将参数转换为数字，因此非数字总是返回 false。以下都返回 false：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot;NaN&quot;</span>); </span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="literal">undefined</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(&#123;&#125;);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot;blabla&quot;</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="literal">true</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="literal">null</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot;37&quot;</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot;37.37&quot;</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot;&quot;</span>);</span><br><span class="line"><span class="title class_">Number</span>.<span class="built_in">isNaN</span>(<span class="string">&quot; &quot;</span>);</span><br></pre></td></tr></table></figure>
<p>全局 isNaN() 函数会将参数强制转换为数字：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot;NaN&quot;</span>); <span class="comment">// true</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="literal">undefined</span>); <span class="comment">// true</span></span><br><span class="line"><span class="built_in">isNaN</span>(&#123;&#125;); <span class="comment">// true</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot;blabla&quot;</span>); <span class="comment">// true</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="literal">true</span>); <span class="comment">// false，强制转换为 1</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="literal">null</span>); <span class="comment">// false，强制转换为 0</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot;37&quot;</span>); <span class="comment">// false，强制转换为 37</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot;37.37&quot;</span>); <span class="comment">// false，强制转换为 37.37</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot;&quot;</span>); <span class="comment">// false，强制转换为 0</span></span><br><span class="line"><span class="built_in">isNaN</span>(<span class="string">&quot; &quot;</span>); <span class="comment">// false，强制转换为 0</span></span><br></pre></td></tr></table></figure>
</details>
]]></content>
      <categories>
        <category>前端</category>
        <category>面经</category>
        <category>类型判断</category>
      </categories>
      <tags>
        <tag>面经</tag>
      </tags>
  </entry>
  <entry>
    <title>动态路由+能支持iframe嵌入</title>
    <url>/2025/09/23/%E5%8A%A8%E6%80%81%E8%B7%AF%E7%94%B1+%E8%83%BD%E6%94%AF%E6%8C%81iframe%E5%B5%8C%E5%85%A5/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>动态路由+能支持iframe嵌入</h1>
<p>“动态路由 + iframe 内嵌”的工作机制与关键点，帮你快速看清楚从配置→路由注入→渲染的全流程。</p>
<h3 id="总体流程">总体流程</h3>
<ul class="lvl-0">
<li class="lvl-2">
<p>静态和动态菜单源合并后统一转换为可注入的路由对象并注册到路由树。</p>
</li>
<li class="lvl-2">
<p>若路由项的 <code>meta.type</code> 指定为支持内嵌的类型（本项目现有：<code>iframe</code>&lt;打开新标签页&gt; 与 <code>embedding_iframe</code>&lt;页面内嵌&gt;），会被转换为一个内部访问路径 <code>/i/$&#123;name&#125;</code>，并将原始外链地址存入 <code>meta.url</code>。</p>
</li>
<li class="lvl-2">
<p>页面渲染时，真实的页面不是常规 <code>view</code> 组件，而是通过 <code>iframeView</code> 组件读取 <code>meta.url</code>，以 <code>&lt;iframe&gt;</code> 内嵌方式展示。</p>
</li>
</ul>
<h3 id="关键文件与职责">关键文件与职责</h3>
<ul class="lvl-0">
<li class="lvl-2">
<p>路由注入与转换：<code>src/router/index.js</code></p>
<ul class="lvl-2">
<li class="lvl-4">合并菜单源：
<ul class="lvl-4">
<li class="lvl-6">静态：<code>userRoutes</code>（来自 <code>src/config/route.js</code>）</li>
<li class="lvl-6">动态：<code>MENU</code>（来自 <code>tool.data.get('MENU')</code>）</li>
</ul>
</li>
<li class="lvl-4">鉴权过滤：<code>treeFilter(userRoutes, func)</code> 会按 <code>meta.role</code> 做权限控制。</li>
<li class="lvl-4">转换核心：<code>filterAsyncRouter(routerMap)</code>
<ul class="lvl-4">
<li class="lvl-6">识别外链型菜单并改造为内嵌路由：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//处理外部链接特殊路由</span><br><span class="line">if (item.meta.type == &#x27;iframe&#x27; || item.meta.type == &#x27;embedding_iframe&#x27;) &#123;</span><br><span class="line">  item.meta.url = item.path;</span><br><span class="line">  item.path = `/i/$&#123;item.name&#125;`;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul class="lvl-6">
<li class="lvl-8">原 <code>path</code>（外链 URL）被保存到 <code>meta.url</code></li>
<li class="lvl-8">新 <code>path</code> 统一变成 <code>/i/$&#123;name&#125;</code></li>
</ul>
</li>
<li class="lvl-6">统一生成标准路由对象并按 <code>layout</code> 父路由动态注册：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var route = &#123;</span><br><span class="line">  path: item.path,</span><br><span class="line">  name: item.name,</span><br><span class="line">  meta: item.meta,</span><br><span class="line">  redirect: item.redirect,</span><br><span class="line">  children: item.children ? filterAsyncRouter(item.children) : null,</span><br><span class="line">  component: loadComponent(item.component),</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li class="lvl-4">首次进入时的动态注册控制：<code>isGetRouter</code> 保证只注入一次；注入完成后才加 404。</li>
<li class="lvl-4">注意：如果存在“进入前打开新标签”的逻辑，通常写在 <code>beforeEach</code> 中根据 <code>meta.type==='iframe'</code> 执行 <code>window.open(...)</code>。若你要所有 iframe 内嵌显示，应避免在守卫里拦截打开新页，仅依赖 <code>iframeView</code> 渲染。</li>
</ul>
</li>
<li class="lvl-2">
<p>菜单与链接规范化：<code>src/layout/index.vue</code></p>
<ul class="lvl-2">
<li class="lvl-4">菜单二次处理 <code>filterUrl</code>：确保左侧菜单点击时，外链类项的 <code>path</code> 也会落在 <code>/i/$&#123;name&#125;</code>，与路由规则一致。<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (item.meta.type == &#x27;iframe&#x27; || item.meta.type == &#x27;embedding_iframe&#x27;) &#123;</span><br><span class="line">  item.path = `/i/$&#123;item.name&#125;`;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li class="lvl-2">
<p>iframe 渲染容器：<code>src/layout/components/iframeView.vue</code></p>
<ul class="lvl-2">
<li class="lvl-4">只在当前路由 <code>meta.type</code> 为外链内嵌类型时显示 iframe 容器：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;div v-show=&quot;$route.meta.type==&#x27;iframe&#x27; || $route.meta.type==&#x27;embedding_iframe&#x27;&quot; class=&quot;iframe-pages&quot;&gt;</span><br><span class="line">  &lt;iframe v-for=&quot;item in iframeList&quot; :key=&quot;item.meta.url&quot; v-show=&quot;$route.meta.url==item.meta.url&quot; :src=&quot;item.meta.url&quot; frameborder=&#x27;0&#x27;&gt;&lt;/iframe&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>
</li>
<li class="lvl-4">进入外链路由时，会将当前路由压入 <code>iframeList</code>，从而支持多标签/刷新等行为：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if(route.meta.type == &#x27;iframe&#x27; || route.meta.type == &#x27;embedding_iframe&#x27;)&#123;</span><br><span class="line">  if(this.ismobile || !this.layoutTags)&#123;</span><br><span class="line">    this.$store.commit(&quot;setIframeList&quot;, route)</span><br><span class="line">  &#125;else&#123;</span><br><span class="line">    this.$store.commit(&quot;pushIframeList&quot;, route)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;else&#123;</span><br><span class="line">  if(this.ismobile || !this.layoutTags)&#123;</span><br><span class="line">    this.$store.commit(&quot;clearIframeList&quot;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li class="lvl-4">对应的持久化列表在 <code>src/store/modules/iframe.js</code>，包含 <code>setIframeList/pushIframeList/refreshIframe/clearIframeList</code> 等。</li>
</ul>
</li>
</ul>
<h3 id="路由配置怎么写（例）">路由配置怎么写（例）</h3>
<p>在 <code>src/config/route.js</code> 静态菜单里，外链菜单这样写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">	<span class="attr">name</span>: <span class="string">&#x27;compassindustry&#x27;</span>,</span><br><span class="line">	<span class="attr">path</span>: <span class="string">&#x27;https://open.ys7.com/cn/s/index&#x27;</span>,</span><br><span class="line">	<span class="attr">meta</span>: &#123; <span class="attr">title</span>: <span class="string">&#x27;行业指数&#x27;</span>, <span class="attr">icon</span>: <span class="string">&#x27;el-icon-caret-right&#x27;</span>, <span class="attr">type</span>: <span class="string">&#x27;embedding_iframe&#x27;</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul class="lvl-0">
<li class="lvl-2">
<p><code>name</code>：必须唯一，用来生成内部访问路径 <code>/i/compassindustry</code></p>
</li>
<li class="lvl-2">
<p><code>path</code>：外部真实 URL</p>
</li>
<li class="lvl-2">
<p><code>meta.type</code>：使用 <code>iframe</code> 或 <code>embedding_iframe</code> 任一都可按当前代码支持内嵌</p>
</li>
<li class="lvl-2">
<p>渲染流程：点击菜单 → 实际跳转到 <code>/i/compassindustry</code> → <code>iframeView</code> 根据 <code>meta.url</code> 内嵌加载外链</p>
</li>
</ul>
<h3 id="常见问题与排查">常见问题与排查</h3>
<ul class="lvl-0">
<li class="lvl-2">
<p>“No match found for location with path ‘/i/xxx’”：通常是该项路由尚未被注入。确保：</p>
<ul class="lvl-2">
<li class="lvl-4">该菜单项包含在 <code>userRoutes</code> 或后端返回的 <code>MENU</code> 中；</li>
<li class="lvl-4">名称 <code>name</code> 与菜单里点击的一致（由 <code>name</code> 决定 <code>/i/$&#123;name&#125;</code>）；</li>
<li class="lvl-4">首次加载时不要过早跳转到尚未注册的 <code>/i/...</code>，或保证动态注入在导航前完成。</li>
</ul>
</li>
<li class="lvl-2">
<p>点击外链被新开标签页：检查路由守卫里是否有针对 <code>meta.type === 'iframe'</code> 的 <code>window.open</code> 拦截。若你希望“全部内嵌”，请移除或改为仅对特定类型生效。</p>
</li>
<li class="lvl-2">
<p>需要新支持的类型：在 <code>filterAsyncRouter</code> 与 <code>iframeView.vue</code> 的判断里加上你的 <code>meta.type</code> 即可。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>vue中的插槽slot</title>
    <url>/2025/07/15/vue%E4%B8%AD%E6%8F%92%E6%A7%BDslot/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>vue中的插槽slot</h1>
<p>为了让组件具备更强的通用性</p>
<h2 id="子组件">子组件</h2>
<p>声明插槽的Comp组件</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">slot</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span> //默认插槽，调用default()，将返回的虚拟节点放到这里</span><br><span class="line">    <span class="tag">&lt;<span class="name">slot</span> <span class="attr">name</span>=<span class="string">&#x27;slot1&#x27;</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span>  //具名插槽，调用slot1()，将返回的虚拟节点放到这里</span><br><span class="line">    <span class="tag">&lt;<span class="name">slot</span> <span class="attr">name</span>=<span class="string">&#x27;slot2&#x27;</span> <span class="attr">msg</span>=<span class="string">&#x27;hello world&#x27;</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span>  //作用域插槽，调用slot2(&#123;msg:&#x27;hello world&#x27;&#125;)，将返回的虚拟节点放到这里</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="父组件">父组件</h2>
<p>使用具有插槽的子组件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;Comp&gt;</span><br><span class="line">    &lt;p&gt;</span><br><span class="line">        default slot</span><br><span class="line">    &lt;/p&gt;</span><br><span class="line">    &lt;template #slot1&gt;</span><br><span class="line">      &lt;p&gt;</span><br><span class="line">        slot1</span><br><span class="line">      &lt;/p&gt;</span><br><span class="line">    &lt;/template&gt;</span><br><span class="line">    &lt;template #slot2=&#x27;slotData&#x27;&gt;</span><br><span class="line">      &lt;p&gt;</span><br><span class="line">        slot2:&#123;&#123;slotData.msg&#125;&#125;</span><br><span class="line">      &lt;/p&gt;</span><br><span class="line">    &lt;/template&gt;</span><br><span class="line">&lt;/Comp&gt;</span><br></pre></td></tr></table></figure>
<p>插槽向组件Comp传递一个对象，对象里面的属性名就是插槽名</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">default</span>:<span class="keyword">function</span>(<span class="params"></span>)&#123;&#125;,</span><br><span class="line">    <span class="attr">slot1</span>:<span class="keyword">function</span>(<span class="params"></span>)&#123;&#125;,</span><br><span class="line">    <span class="attr">slot2</span>:<span class="keyword">function</span>(<span class="params">slotData</span>)&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//调用这些函数就会返回对应的虚拟节点</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
        <category>vue中的插槽slot</category>
      </categories>
      <tags>
        <tag>vue</tag>
      </tags>
  </entry>
  <entry>
    <title>微信小程序-学习笔记（学习中）</title>
    <url>/2024/10/15/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="1-图片懒加载没生效问题">1. 图片懒加载没生效问题</h2>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">image</span> <span class="attr">src</span>=<span class="string">&quot;/assets/xxx.png&quot;</span> <span class="attr">mode</span>=<span class="string">&quot;&quot;</span> <span class="attr">show-menu-by-longpress</span> <span class="attr">lazy-load</span> /&gt;</span></span><br></pre></td></tr></table></figure>
<img data-src="/2024/10/15/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image.png" class="" title="图片懒加载没生效问题">
<p>lazy-load的图片不是出现在屏幕上才会开始加载，而是提前3个屏幕高度就会加载。所以说三个屏幕高度以内的图片是不会懒加载的，上下三屏即为三个屏幕高度。</p>
<h2 id="2-text-组件">2. text 组件</h2>
<img data-src="/2024/10/15/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-1.png" class="" title="text 组件">
<h2 id="3-navigator组件中的-open-type">3. navigator组件中的 open-type</h2>
<img data-src="/2024/10/15/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-2.png" class="" title="navigator中的 open-type">
<p>注意：</p>
<blockquote>
<p>在进行页面跳转时，需要在路径的前面添加 / 斜线，否则跳转不成功<navigator url="/pages/list/list?id=10&num=hue" open-type="navigate">到商品列表页面</navigator></p>
</blockquote>
<ul class="lvl-0">
<li class="lvl-2">
<p>navigate：只能跳转到非 TabBar 页面，不能跳转到 TabBar 页面，保留上一级页面</p>
</li>
<li class="lvl-2">
<p>redirect：只能跳转到非 TabBar 页面，不能跳转到 TabBar 页面，关闭上一级页面</p>
</li>
<li class="lvl-2">
<p>switchTab：只能跳转到 TabBar 页面，不能跳转到非 TabBar 页面，关闭其他所有的非 TabBar 页面</p>
</li>
<li class="lvl-2">
<p>reLaunch：关闭所有页面，然后打开小程序中某一个页面</p>
</li>
<li class="lvl-2">
<p>navigateBack返回上一页或者返回前几页，默认只能返回上一页</p>
<ul class="lvl-2">
<li class="lvl-6">delte：返回的层级，默认是 1，如果想返回几级，就写几</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>微信小程序</category>
      </categories>
      <tags>
        <tag>微信小程序</tag>
      </tags>
  </entry>
  <entry>
    <title>本地mock的使用</title>
    <url>/2025/08/29/%E6%9C%AC%E5%9C%B0mock%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>mock的使用</h1>
<img data-src="/2025/08/29/%E6%9C%AC%E5%9C%B0mock%E7%9A%84%E4%BD%BF%E7%94%A8/image-1.png" class="" title="mock 使用地方">
<ul class="lvl-0">
<li class="lvl-2">
<p>技术栈</p>
<ul class="lvl-2">
<li class="lvl-4">TypeScript+Hono</li>
<li class="lvl-4">@faker-js/faker （生成虚拟数据的工具）</li>
</ul>
</li>
</ul>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">npm create hono@latest my-app</span><br><span class="line">npm install @hono/node-server</span><br><span class="line">npm install @faker-js/faker --save-dev</span><br></pre></td></tr></table></figure>
<img data-src="/2025/08/29/%E6%9C%AC%E5%9C%B0mock%E7%9A%84%E4%BD%BF%E7%94%A8/image-2.png" class="" title="创建hono项目">
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// index.ts</span></span><br><span class="line"><span class="keyword">import</span> &#123; serve &#125; <span class="keyword">from</span> <span class="string">&quot;@hono/node-server&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Hono</span> &#125; <span class="keyword">from</span> <span class="string">&quot;hono&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; cors &#125; <span class="keyword">from</span> <span class="string">&quot;hono/cors&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; proxy &#125; <span class="keyword">from</span> <span class="string">&quot;hono/proxy&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; fakerZH_CN &#125; <span class="keyword">from</span> <span class="string">&quot;@faker-js/faker&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> app = <span class="keyword">new</span> <span class="title class_">Hono</span>();</span><br><span class="line"></span><br><span class="line">app.<span class="title function_">use</span>(<span class="title function_">cors</span>());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义的mock接口</span></span><br><span class="line">app.<span class="title function_">get</span>(<span class="string">&quot;/api/user&quot;</span>, <span class="function">(<span class="params">c</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> c.<span class="title function_">json</span>(&#123;</span><br><span class="line">    <span class="attr">data</span>: &#123;</span><br><span class="line">      <span class="attr">name</span>: fakerZH_CN.<span class="property">person</span>.<span class="title function_">fullName</span>(),</span><br><span class="line">      <span class="attr">age</span>: fakerZH_CN.<span class="property">person</span>.<span class="title function_">sex</span>()</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">code</span>: <span class="number">200</span>,</span><br><span class="line">    <span class="attr">message</span>: <span class="string">&#x27;success&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 代理中间件：转发未定义的接口到 localhost:3000</span></span><br><span class="line">app.<span class="title function_">all</span>(<span class="string">&quot;*&quot;</span>, <span class="title function_">async</span> (c) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> targetUrl = <span class="keyword">new</span> <span class="title function_">URL</span>(c.<span class="property">req</span>.<span class="property">url</span>);</span><br><span class="line">  targetUrl.<span class="property">host</span> = <span class="string">&quot;localhost:3000&quot;</span>;<span class="comment">//这个是兜底代理的目标（后端服务器地址）</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">proxy</span>(targetUrl, c.<span class="property">req</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="title function_">serve</span>(</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">fetch</span>: app.<span class="property">fetch</span>,</span><br><span class="line">    <span class="attr">port</span>: <span class="number">3001</span>,<span class="comment">//启动入口</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function">(<span class="params">info</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Server is running on http://localhost:<span class="subst">$&#123;info.port&#125;</span>`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>自己的项目进行代理</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//.env.development</span></span><br><span class="line">VITE_API_BASE_URL=http:<span class="comment">//localhost:3000</span></span><br><span class="line">VITE_MOCK=<span class="literal">true</span></span><br></pre></td></tr></table></figure>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vite.config.ts</span></span><br><span class="line"><span class="keyword">import</span> &#123; defineConfig, loadEnv &#125; <span class="keyword">from</span> <span class="string">&quot;vite&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> vue <span class="keyword">from</span> <span class="string">&quot;@vitejs/plugin-vue&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// https://vite.dev/config/</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineConfig</span>(<span class="function">(<span class="params">&#123; mode &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> env = <span class="title function_">loadEnv</span>(mode, process.<span class="title function_">cwd</span>(), <span class="string">&quot;&quot;</span>);</span><br><span class="line">  <span class="keyword">const</span> target = env.<span class="property">VITE_API_BASE_URL</span>;</span><br><span class="line">  <span class="keyword">const</span> isMock = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(env.<span class="property">VITE_MOCK</span>);</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(target);</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">plugins</span>: [<span class="title function_">vue</span>()],</span><br><span class="line">    <span class="attr">server</span>: &#123;</span><br><span class="line">      <span class="attr">proxy</span>: &#123;</span><br><span class="line">        <span class="string">&quot;/api&quot;</span>: &#123;</span><br><span class="line">          <span class="attr">target</span>: isMock</span><br><span class="line">            ? <span class="string">&quot;http://localhost:3001&quot;</span> <span class="comment">// 本地mock服务</span></span><br><span class="line">            : target,</span><br><span class="line">          <span class="attr">changeOrigin</span>: <span class="literal">true</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">      &#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const userInfo = ref()</span><br><span class="line">const fetchUserInfo = async () =&gt; &#123;</span><br><span class="line">  const res = await fetch(&quot;/api/user&quot;);</span><br><span class="line">  userInfo.value = await res.json();</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
        <category>本地mock的使用</category>
      </categories>
  </entry>
  <entry>
    <title>Vue首屏加载优化</title>
    <url>/2025/07/08/vue%E9%A6%96%E5%B1%8F%E5%8A%A0%E8%BD%BD%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h2 id="怎样在代码中将首屏加载时间计算出来">怎样在代码中将首屏加载时间计算出来</h2>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">onload</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">// 首屏时间的计算</span></span><br><span class="line">    <span class="comment">// console.log(performance.timing.domComplete - performance.timing.navigationStart, &#x27;times&#x27;)//已弃用</span></span><br><span class="line">    </span><br><span class="line">   <span class="keyword">const</span> observer = <span class="keyword">new</span> <span class="title class_">PerformanceObserver</span>(<span class="function">(<span class="params">list</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="variable language_">console</span>.<span class="title function_">log</span>(list.<span class="title function_">getEntries</span>())</span><br><span class="line">    &#125;)</span><br><span class="line">    observer.<span class="title function_">observe</span>(&#123; <span class="attr">entryTypes</span>: [<span class="string">&#x27;navigation&#x27;</span>] &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="vue中的异步组件">vue中的异步组件</h2>
<p>在vue框架中怎么实现当用到这个组件时才会加载</p>
<p>异步组件（在打包的时候单独形成分包）</p>
<h3 id="正常普通导入组件">正常普通导入组件</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> cs <span class="keyword">from</span> <span class="string">&quot;./cs.vue&quot;</span></span><br></pre></td></tr></table></figure>
<h3 id="异步组件">异步组件</h3>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 异步组件</span></span><br><span class="line"><span class="keyword">const</span> cs = <span class="title function_">defineAsyncComponent</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">import</span>(<span class="string">&#x27;./cs.vue&#x27;</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h2 id="首页图片">首页图片</h2>
<p>接口返回的图片为网络图片，在页面加载的时候不会占用加载时间</p>
<h2 id="优化白屏显示-骨架屏">优化白屏显示-骨架屏</h2>
<p>在vue页面没加载出来之前显示骨架屏</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;app&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 35%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 50%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 60%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 80%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 100%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;width: 100%;height: 20px;background-color: #999;margin-bottom: 10px;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
        <category>首屏加载优化</category>
      </categories>
      <tags>
        <tag>vue</tag>
      </tags>
  </entry>
  <entry>
    <title>动画</title>
    <url>/2025/07/19/%E5%8A%A8%E7%94%BB/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h1>再也不能用定时器做动画</h1>
<h2 id="CSS动画">CSS动画</h2>
<p>CSS动画是浏览器原生支持的动画实现方式，具有以下特点：</p>
<ol>
<li class="lvl-3">
<p><strong>硬件加速</strong>：浏览器会自动优化，使用GPU加速</p>
</li>
<li class="lvl-3">
<p><strong>流畅度高</strong>：由浏览器内部调度，与刷新率同步</p>
</li>
<li class="lvl-3">
<p><strong>简单易用</strong>：只需定义关键帧和动画属性</p>
</li>
<li class="lvl-3">
<p><strong>性能优异</strong>：不会阻塞主线程</p>
</li>
</ol>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span> &#123;</span><br><span class="line">  <span class="attribute">animation</span>: move <span class="number">2s</span> ease-in-out infinite;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@keyframes</span> move &#123;</span><br><span class="line">  <span class="number">0%</span> &#123; <span class="attribute">transform</span>: <span class="built_in">translateX</span>(<span class="number">0</span>); &#125;</span><br><span class="line">  <span class="number">100%</span> &#123; <span class="attribute">transform</span>: <span class="built_in">translateX</span>(<span class="number">100px</span>); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="JS动画">JS动画</h2>
<h3 id="计时器动画（setTimeout-setInterval）">计时器动画（setTimeout/setInterval）</h3>
<p>使用计时器实现动画存在严重问题：</p>
<ol>
<li class="lvl-3">
<p><strong>时间不精准</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">受事件循环机制影响，回调可能被延迟执行</li>
<li class="lvl-5">最小间隔时间受限（通常4ms）</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>帧率不稳定</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">无法与屏幕刷新率同步</li>
<li class="lvl-5">可能导致动画卡顿或跳帧</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>性能问题</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">即使页面不可见也会继续执行</li>
<li class="lvl-5">增加不必要的CPU负担</li>
</ul>
</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不推荐的实现方式</span></span><br><span class="line"><span class="keyword">let</span> pos = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">const</span> timer = <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  pos += <span class="number">1</span>;</span><br><span class="line">  element.<span class="property">style</span>.<span class="property">left</span> = pos + <span class="string">&#x27;px&#x27;</span>;</span><br><span class="line">  <span class="keyword">if</span>(pos &gt;= <span class="number">100</span>) <span class="built_in">clearInterval</span>(timer);</span><br><span class="line">&#125;, <span class="number">16</span>); <span class="comment">// 试图模拟60fps</span></span><br></pre></td></tr></table></figure>
<h3 id="requestAnimationFrame-RAF">requestAnimationFrame (RAF)</h3>
<p>RAF是专为动画设计的API，特点包括：</p>
<ol>
<li class="lvl-3">
<p><strong>与刷新率同步</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">自动匹配显示器刷新率（通常60fps）</li>
<li class="lvl-5">每次重绘前调用回调函数</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>高效节能</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">页面不可见时自动暂停</li>
<li class="lvl-5">浏览器可以优化执行</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>更流畅的动画</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">避免过度渲染</li>
<li class="lvl-5">减少不必要的计算</li>
</ul>
</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">animate</span>(<span class="params"></span>) &#123;</span><br><span class="line">  element.<span class="property">style</span>.<span class="property">left</span> = (pos += <span class="number">1</span>) + <span class="string">&#x27;px&#x27;</span>;</span><br><span class="line">  <span class="keyword">if</span>(pos &lt; <span class="number">100</span>) &#123;</span><br><span class="line">    <span class="title function_">requestAnimationFrame</span>(animate);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">requestAnimationFrame</span>(animate);</span><br></pre></td></tr></table></figure>
<h3 id="Web-Animations-API">Web Animations API</h3>
<p>现代浏览器提供的强大动画API：</p>
<ol>
<li class="lvl-3">
<p><strong>声明式语法</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">element.<span class="title function_">animate</span>([</span><br><span class="line">  &#123; <span class="attr">transform</span>: <span class="string">&#x27;translateX(0)&#x27;</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">transform</span>: <span class="string">&#x27;translateX(100px)&#x27;</span> &#125;</span><br><span class="line">], &#123;</span><br><span class="line">  <span class="attr">duration</span>: <span class="number">1000</span>,</span><br><span class="line">  <span class="attr">easing</span>: <span class="string">&#x27;ease-in-out&#x27;</span>,</span><br><span class="line">  <span class="attr">iterations</span>: <span class="title class_">Infinity</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
<li class="lvl-3">
<p><strong>优势特性</strong>：</p>
<ul class="lvl-2">
<li class="lvl-5">与CSS动画相同的性能优势</li>
<li class="lvl-5">提供精细控制（暂停、反转、变速等）</li>
<li class="lvl-5">可以监听动画事件</li>
<li class="lvl-5">返回Animation对象便于控制</li>
</ul>
</li>
<li class="lvl-3">
<p><strong>时间轴控制</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> animation = element.<span class="title function_">animate</span>(<span class="comment">/* ... */</span>);</span><br><span class="line">animation.<span class="title function_">pause</span>(); <span class="comment">// 暂停</span></span><br><span class="line">animation.<span class="title function_">play</span>();  <span class="comment">// 播放</span></span><br><span class="line">animation.<span class="title function_">cancel</span>(); <span class="comment">// 取消</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="最佳实践建议">最佳实践建议</h2>
<ol>
<li class="lvl-3">
<p><strong>优先使用CSS动画</strong>：简单动画首选CSS实现</p>
</li>
<li class="lvl-3">
<p><strong>复杂动画使用Web Animations API</strong>：需要程序控制时选择</p>
</li>
<li class="lvl-3">
<p><strong>必要时使用RAF</strong>：需要完全控制动画流程时</p>
</li>
<li class="lvl-3">
<p><strong>避免使用定时器动画</strong>：性能差且不精准</p>
</li>
</ol>
<p>现代浏览器提供了多种高性能动画方案，开发者应该摒弃传统的定时器动画方式，选择更高效、更精准的动画实现技术。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>动画</category>
      </categories>
      <tags>
        <tag>面经</tag>
      </tags>
  </entry>
  <entry>
    <title>带参数的文件上传</title>
    <url>/2024/10/26/%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><hr>
<span id="more"></span>
<h2 id="Element-plus-组件upload-关闭自动上传">Element plus 组件upload 关闭自动上传</h2>
<img data-src="/2024/10/26/%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/image-1.png" class="" title="带参数的文件上传">
<h2 id="成功后的回调">成功后的回调</h2>
<img data-src="/2024/10/26/%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/image-2.png" class="" title="带参数的文件上传">
<h2 id="获取实际文件对象">获取实际文件对象</h2>
<img data-src="/2024/10/26/%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/image-3.png" class="" title="带参数的文件上传">
<h2 id="上传方法">上传方法</h2>
<h3 id="使用FormData-对象">使用FormData()对象</h3>
<p><strong>FormData</strong> 接口提供了一种表示表单数据的键值对 <code>key/value</code> 的构造方式，并且可以轻松的将数据通过<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/XMLHttpRequest/send"><code>XMLHttpRequest.send()</code></a> 方法发送出去，本接口和此方法都相当简单直接。如果送出时的编码类型被设为 <code>&quot;multipart/form-data&quot;</code>，它会使用和表单一样的格式。</p>
<h3 id="例子">例子</h3>
<p>下面的代码将创建一个空的 FormData 对象：</p>
<p>jsCopy to Clipboard</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> formData = <span class="keyword">new</span> FormData(); <span class="comment">// 当前为空</span></span><br></pre></td></tr></table></figure>
<p>你可以使用<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FormData/append"><code>FormData.append</code></a>来添加键/值对到表单里面；</p>
<p>jsCopy to Clipboard</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">formData.<span class="built_in">append</span>(<span class="string">&quot;username&quot;</span>, <span class="string">&quot;Chris&quot;</span>);</span><br></pre></td></tr></table></figure>
<img data-src="/2024/10/26/%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/image-4.png" class="" title="带参数的文件上传">]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>前端</tag>
      </tags>
  </entry>
</search>
